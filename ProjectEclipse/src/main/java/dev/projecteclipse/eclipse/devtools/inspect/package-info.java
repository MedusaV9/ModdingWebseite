/**
 * Operator inspection GUIs (F-066): the live {@code /invsee} and {@code /enderchestsee} views.
 * They reuse the vanilla {@code GENERIC_9x6} / {@code GENERIC_9x3} screens, so no client-side
 * code and no custom {@code MenuType} registration are involved.
 */
package dev.projecteclipse.eclipse.devtools.inspect;
