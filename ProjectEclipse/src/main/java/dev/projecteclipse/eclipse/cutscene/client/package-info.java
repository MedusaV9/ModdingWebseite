/**
 * Client side of the cutscene engine: the synced path library and (from the camera-director
 * commit) the camera flight, letterbox, input swallow and skip handling. Classes here are
 * only ever resolved from client-dist payload handlers / client event subscribers.
 */
package dev.projecteclipse.eclipse.cutscene.client;
