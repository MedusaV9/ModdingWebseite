# PROJECT: ECLIPSE v2 — Sequenced Worker Prompts

16 workers, STRICTLY SEQUENTIAL on the shared checkout `/workspace/ProjectEclipse`.
Never run two workers at once. Each worker prompt below is self-contained; the orchestrator
MUST prepend the **Common Preamble** verbatim to every worker prompt.

Model recommendations: `fable-xhigh` unless marked `sol-fast` (only well-specified, isolated tasks).

---

## COMMON PREAMBLE (prepend to every worker prompt)

> You are working on **PROJECT: ECLIPSE v2**, a NeoForge **1.21.1** mod at
> `/workspace/ProjectEclipse` (modid `eclipse`, package `dev.projecteclipse.eclipse`, Mojmap +
> Parchment, ModDevGradle 2.0.142, Java 21, Gradle wrapper 9.2.1). v1 is complete and building
> green. Before writing any code, READ: `README.md` (full v1 architecture + frozen "Core APIs"),
> `AGENTS.md` (dev-environment rules), `docs/PLAN_V2.md` (v2 architecture — your task is one
> worker slice of it), and the specific `docs/ideas/*.md` sections cited in your task.
> Rules:
> - Work ONLY inside `/workspace/ProjectEclipse`. Use `./gradlew` (no system Gradle). The first
>   build may download toolchains (allow long timeouts); later builds are fast.
> - `./gradlew build` MUST be green before you commit. Fix every compile error you introduce.
> - Do NOT regress v1 features (lifesteal/lives, limbo + ghost ship, day scheduler, modgate,
>   anonymity suite, SVC mute, artifact menu, altar + revive ritual, admin commands, custom
>   title screen, purple sky). Only touch the v1 files your task lists; if you must touch
>   another, keep its public behavior identical and say so in your final message.
> - Server smoke test where your task says so: write `run/eula.txt` containing `eula=true`,
>   `./gradlew runServer`, wait for `Done`, exercise the listed commands, then stop the server
>   by SPECIFIC PID (`ps -eo pid,args | grep devlaunch.Main`, `kill <pid>`) — never pkill.
>   Note: piping runServer output means console `stop` won't work; kill by PID.
> - Keep the existing code style: `package-info.java` per package, `@EventBusSubscriber`
>   pattern, action-bar hints (never chat spam), en_us + de_de lang entries for every new
>   user-visible string, server-authoritative logic with S2C sync via `network/` payloads.
> - New textures/sounds you need: write PROGRAMMER-ART placeholders generated by code — put
>   single-file Java generators under `scripts/placeholder_gen/` (run with
>   `java scripts/placeholder_gen/YourGen.java`, uses `javax.imageio`), commit both scripts and
>   PNGs. Sounds: commit tiny valid silent/synth `.ogg` placeholders (copy the pattern of
>   `assets/eclipse/sounds/event/submerge.ogg`). Real art arrives later at the exact same paths
>   (see `docs/ASSET_MANIFEST_V2.md` — respect its paths/dimensions when creating files).
> - When you change a public API or add config/payloads, update the matching `README.md`
>   section in the same commit.
> - Finish with: `./gradlew build` green → `git add -A` (inside ProjectEclipse) → one commit
>   per logical change using the exact message(s) given → `git push` only if instructed by the
>   orchestrator environment; otherwise leave commits local.

---

## Worker 1 — Veil 4.3.0 foundation: dependency, Iris gate, post shaders, Quasar

**Model:** fable-xhigh

**Prompt:**

Your job: make Veil 4.3.0 a required dependency and build the v2 VFX foundation. Read
`docs/ideas/02_veil_vfx.md` IN FULL first — it contains verified Gradle coordinates, asset
paths, JSON schemas, and GLSL you must use.

Tasks:
1. **First commit — planning docs**: `docs/` is currently untracked. `git add docs && git commit -m "docs: v2 research reports + master plan"` before any code.
2. **Gradle**: add repositories `https://maven.blamejared.com` (Veil). Dependency:
   `implementation("foundry.veil:veil-neoforge-1.21.1:4.3.0") { exclude group: "maven.modrinth"; exclude group: "me.fallenbreath" }`.
   Attempt jar-in-jar via ModDevGradle's `jarJar` configuration with range `[4.3.0,)`
   (prefer 4.3.0). If jarJar wiring fails after 2 attempts, fall back to plain
   `implementation` and note in README that the Veil jar ships alongside (W16 downloads it).
   Add `veil_version=4.3.0` to `gradle.properties`.
3. **mods.toml**: in `src/main/templates/META-INF/neoforge.mods.toml` add a `required`
   dependency on modId `veil`, versionRange `[4.3.0,)`, ordering AFTER, side BOTH.
4. **Client config**: new `dev.projecteclipse.eclipse.core.config.EclipseClientConfig` using
   NeoForge `ModConfigSpec`, registered in the `EclipseMod` constructor via
   `modContainer.registerConfig(ModConfig.Type.CLIENT, ...)`. Booleans (defaults true):
   `customMenu`, `showBossbarSkin`, `showSidebar`, `uiSounds`, `customCursor`, `veilPostFx`,
   `reducedFx` (default false). Later workers read these — expose static getters.
5. **Iris gate**: replace v1 reflection `client/sky/IrisCompat.java` with Veil's first-party
   `foundry.veil.api.compat.IrisCompat` (`INSTANCE` may be null without Iris;
   `areShadersLoaded()` = shaderpack active). Keep the class name/location as a thin wrapper
   (`EclipseIrisState.shaderPackActive()`) so v1 callers (`OverworldPurpleEffects`,
   `LimboSpecialEffects`, `SkyRenderUtil` — check usages) need minimal edits.
6. **`veilfx` package** (client-only): `VeilPostController` — owns pipeline toggling via
   `VeilRenderSystem.renderer().getPostProcessingManager().add(id)/.remove(id)/.isActive(id)`;
   per-frame uniforms via
   `VeilEventPlatform.INSTANCE.preVeilPostProcessing((name, pipeline, ctx) -> pipeline.getUniform("X").setFloat(...))`.
   HARD RULE: every `add(...)` is gated behind `!shaderPackActive() && EclipseClientConfig.veilPostFx()`,
   and every Veil call is try/caught (log once, disable pipeline on repeat failure).
7. **Limbo post shader** (auto-on in limbo): create
   `assets/eclipse/pinwheel/post/limbo.json`, `assets/eclipse/pinwheel/shaders/program/limbo.json`
   and `.fsh` EXACTLY per `docs/ideas/02_veil_vfx.md` §B.1 (purple grade + vignette, `Intensity`
   uniform). On client tick/dimension change: in `eclipse:limbo` → add pipeline and fade
   Intensity 0→1 over ~40 ticks using `foundry.veil.api.client.util.Easing.EASE_OUT_QUAD`;
   else remove. Keep the biome water/fog colors as the shaderpack-active fallback.
8. **Sun purple rim** (auto-on in overworld): `pinwheel/post/sun_halo.json` + program per
   §B.2 — depth-masked additive rim where `DiffuseDepthSampler >= 1.0`, `SunDirection`
   uniform from `level.getSunAngle(partialTick)`, screen projection via
   `#veil:buffer veil:camera VeilCamera`. Active only in overworld, same gate. Keep the v1
   `sun.png` override as fallback.
9. **Quasar systems**: create emitters under `assets/eclipse/quasar/emitters/` (+ modules) for:
   `altar_beam`, `arm_wisps`, `map_expand_materialize`, `border_glitch`, `boss_slam`,
   `heart_burst`, `limbo_motes`, `cutscene_veil` — specs in §C (shapes, modules, Molang).
   New `veilfx/QuasarSpawner`: client helper wrapping
   `ParticleSystemManager mgr = VeilRenderSystem.renderer().getParticleManager(); ParticleEmitter e = mgr.createEmitter(rl); e.setPosition(pos); mgr.addParticleSystem(e);`
   (also `setAttachedEntity`), try/caught. New payload `S2CQuasarPayload(ResourceLocation emitterId, Vec3 pos)`
   (register in `EclipsePayloads`, registrar version → "2"; client handler → QuasarSpawner).
10. **Replace vanilla particle call sites**: `ritual/BeamEmitter.java` (altar beam →
    `altar_beam` via S2CQuasarPayload), `client/ArmParticles.java` (→ `arm_wisps`
    entity-attached), `limbo/StartEventCutscene.java` (submerge/emerge bursts →
    `cutscene_veil`). Keep the old vanilla-particle code path as a 3-line fallback when the
    Quasar spawn throws.

v1 files you may modify: `build.gradle`, `gradle.properties`,
`src/main/templates/META-INF/neoforge.mods.toml`, `EclipseMod.java`,
`client/sky/IrisCompat.java` (replace), `client/sky/*` callers, `ritual/BeamEmitter.java`,
`client/ArmParticles.java`, `limbo/StartEventCutscene.java`, `network/EclipsePayloads.java`,
`README.md`.

Acceptance: `./gradlew build` green; `runServer` boots to Done with Veil in the modlist
(check log) and shuts down clean; grep proves no `reflection` Iris path remains; pipelines
never `add(...)` unguarded (grep for `getPostProcessingManager().add`).

Commits (in order):
`docs: v2 research reports + master plan`
`eclipse v2: veil 4.3.0 required dep + iris gate + client config`
`eclipse v2: limbo + sun-halo post pipelines, quasar emitters, particle call sites`

---

## Worker 2 — Hearts system: real max health + HUD shatter burst

**Model:** sol-fast

**Prompt:**

Your job: replace abstract "lives" with REAL max-health hearts (start 5 hearts = 10 HP) plus
a heart-shatter HUD animation after a respawn with loss. Read `docs/ideas/03_ui_ux.md` §A
("Health bar") and §C in full; read `README.md` "Core APIs" (LivesApi, attachments, payloads).

Semantics: the existing `eclipse:lives` attachment (default 5, copyOnDeath) now MEANS hearts.
`LivesApi` stays the single source of truth and keeps its signatures.

Tasks:
1. New `dev.projecteclipse.eclipse.hearts.HeartsService` (game-bus `@EventBusSubscriber`):
   `apply(ServerPlayer)` sets a transient MAX_HEALTH modifier:
   `player.getAttribute(Attributes.MAX_HEALTH).addOrUpdateTransientModifier(new AttributeModifier(ResourceLocation.fromNamespaceAndPath("eclipse","hearts"), hearts * 2 - 20, AttributeModifier.Operation.ADD_VALUE))`
   (5 hearts ⇒ −10 ⇒ 10 max HP). Clamp current health to new max (min 1.0 while alive).
   Reapply on `PlayerEvent.PlayerRespawnEvent`, `PlayerEvent.Clone`, `PlayerLoggedInEvent`.
   Constant `MAX_HEARTS = 7` (Vitae Shard cap, used by a later worker) and `MIN_HEARTS = 0`.
   Hook `LivesApi.set/add` to call `apply` after mutation (edit `core/state/LivesApi.java`).
2. Heart-burst payload: `S2CHeartBurstPayload(int heartIndex)` in `network/` (playToClient,
   register in `EclipsePayloads`). Send from `lives/LifecycleEvents` respawn path when the
   respawned player has fewer hearts than before death (heartIndex = new hearts value, i.e.
   the first now-missing heart, 0-based).
3. Client `hearts/client/HeartBurstOverlay`: register with
   `event.registerAbove(VanillaGuiLayers.PLAYER_HEALTH, ...)` in `client/EclipseGuiLayers`.
   Do NOT replace the vanilla health layer. Position with vanilla math:
   `x = guiWidth()/2 - 91 + heartIndex * 8`, `y = guiHeight() - gui.leftHeight + <row offset>`
   — read NeoForge's public `Gui.leftHeight` at render time; recompute row stacking if
   `max+absorb > 20` (row step `max(10 - (rows - 2), 3)`). Mirror the vanilla low-health
   jitter seed (`tickCount * 312871`) so the overlay tracks the wiggle. Animation ~12 ticks:
   2t white flash → 3 crack frames → 6 shard quads flying out with gravity + alpha fade.
   Textures: `assets/eclipse/textures/gui/hearts/burst_sheet.png` (6 frames 36×36 in a row,
   216×36) — commit programmer-art via a `scripts/placeholder_gen/` generator. Also trigger
   the Quasar `heart_burst` emitter at the player (via `veilfx.QuasarSpawner`, try/caught).
4. Sound: register `eclipse:ui.heart_shatter` in `registry/EclipseSounds` + `sounds.json`
   + placeholder ogg; play client-side with the burst. 2-tick red vignette flash.
5. Low-hearts heartbeat (small bonus, §F.9): at ≤2 hearts, a subtle pulsing vignette on the
   same overlay + muffled thump sound every ~40t, honoring `EclipseClientConfig.reducedFx()`.
6. Update `README.md` Core APIs (LivesApi now drives max health; document MAX_HEARTS=7).

v1 files you may modify: `core/state/LivesApi.java`, `lives/LifecycleEvents.java`,
`client/EclipseGuiLayers.java`, `network/EclipsePayloads.java`, `registry/EclipseSounds.java`,
`assets/eclipse/sounds.json`, lang files, `README.md`.
Do NOT touch `BanService` ban-at-0 logic — 0 hearts still bans.

Acceptance: build green; `runServer` smoke: join is not required — instead verify via logs +
code review that modifiers are transient (never serialized); `/eclipse lives set <p> 3` path
compiles against unchanged signatures. Kill/respawn/relog cycle will be visually verified in
W16; your gate is build + server boot + `/eclipse lives` command tree still registering.

Commit: `eclipse v2: hearts = real max health + HUD shatter burst overlay`

---

## Worker 3 — Disc world core: chunk generator, biome sectors, dimension height

**Model:** fable-xhigh

**Prompt:**

Your job: turn the overworld into a deterministic floating disc world and prepare the nether
disc profile. Read `docs/ideas/01_world_terrain.md` IN FULL — it is your spec. Key decision
already made (see `docs/PLAN_V2.md` §5): control data is an authored JSON
(`config/eclipse/disc_map.json` defaults written by code, like `EclipseConfig` does) + a fixed
seed constant — NOT painted PNGs (keep a `loadHeightmapOverride(Path)` hook that reads an
optional PNG if present, else procedural).

Tasks:
1. New package `worldgen/`: `DiscMapData` (loads `disc_map.json`: angular biome sector wedges,
   landmark list with positions/radii, mountain center+peak, river polylines, whisper wells;
   plus `public static final long ECLIPSE_SEED = 0xEC1195E0021L;`), `DiscProfile` (overworld:
   surface y 65–90, lens thickness 200 center → 150 rim, underside ~y −130, bedrock bottom 3
   layers, mountain peak y≈280; nether: center y 32–160, thickness ~120, netherrack/blackstone
   palettes, lava moat ring r=50 with two bridge gaps), `DiscTerrainFunction` — PURE function
   `BlockState stateAt(DiscProfile, int x, int y, int z, int stage)`:
   - radius per stage: stage 0 = main disc r=96 + team discs r=24 on ring r=170; 1 → r=225;
     2 → 300; 3 → 360; 4 → 420; 5 → 480 (read from `stages.json` when W4 lands; hardcode a
     `StageRadii` holder now with these defaults and a setter W4 will drive).
   - rim: `edgeNoise = simplex(angle*6)*8` + smoothstep over last 12 blocks (crumbly rim).
   - lens: `bottomY(r) = centerBottom + (rimBottom − centerBottom) * (r/R)²`; bedrock cap
     bottom 3 layers; deepslate stalactite fringe via 2D noise on the underside.
   - surface: procedural fixed-seed heightmap (layered simplex, sector-aware amplitude,
     mountain bump from disc_map.json) ±3 detail noise.
   - deterministic Perlin-worm caves (fixed seed, clamped ≥4 blocks above underside) and
     ring-budgeted ore veins (hashed positions; diamonds only y<−40, biased to center).
   All noise seeded ONLY from ECLIPSE_SEED — never the world seed.
2. `DiscChunkGenerator extends ChunkGenerator`: `MapCodec` via `codec()`; registered in new
   `registry/EclipseWorldgen` with `DeferredRegister` on `Registries.CHUNK_GENERATOR`
   (id `eclipse:disc`) — wire `EclipseWorldgen.register(modEventBus)` in `EclipseMod`.
   Implement `fillFromNoise` (write `LevelChunkSection`s per column from
   DiscTerrainFunction at the CURRENT COMMITTED STAGE — read from a static
   `WorldStageAccess` seam defaulting to stage 0; W4 connects it to EclipseWorldState),
   `getBaseHeight`/`getBaseColumn` from the same function (structure placement + spawn
   selection need them), `buildSurface`/`applyCarvers` minimal (carving already in the
   function), `getSeaLevel`, `getMinY`, `getGenDepth`, `spawnOriginalMobs` no-op.
3. `DiscBiomeSource extends BiomeSource`: codec on `Registries.BIOME_SOURCE`
   (`eclipse:disc_sectors`), returns biomes from disc_map.json angular wedges: center
   meadow/alpine (mountain), pie sectors forest / desert (SE) / jungle (SW) / savanna /
   snowy slopes (N) / swamp / dark forest; nether variant: soul sand + basalt sectors.
4. Dimension JSONs: create `data/minecraft/dimension/overworld.json` using
   `"generator": {"type": "eclipse:disc", "profile": "overworld", ...}`; EDIT the existing
   `data/minecraft/dimension_type/overworld.json` to `min_y: -176, height: 512` (keep its
   v1 fixed-time/ambient tweaks intact). Create `data/minecraft/dimension/the_nether.json`
   with the nether disc profile (generation only — the day-2 unlock stays as-is via
   PhaseInventoryLock). Do NOT use `neoforge:use_server_seed`.
5. Spawn: ensure world spawn lands at disc center (0,0 surface) — override via
   `getBaseHeight` correctness + set spawn in a `ServerStartedEvent` guard if needed.
6. Dev worlds: delete `run/world` (min_y change corrupts old saves) and note in README that
   v2 requires a fresh world.

v1 files you may modify: `EclipseMod.java`, `registry/` (new class),
`data/minecraft/dimension_type/overworld.json`, `README.md`.
Do NOT touch limbo dimension JSONs.

Acceptance: build green. `runServer` with a FRESH world boots to Done; log proves
`eclipse:disc` generator loaded; `/eclipse tp_limbo` + back still works (regression);
teleport far out (`/tp 0 100 400`) shows void beyond stage-0 radius (verify via
`/execute positioned 400 0 400 run setblock ~ ~ ~ stone` succeeding in void air — or simply
document block-at checks with `/execute if block`). Stop server by PID.

Commits:
`eclipse v2: disc terrain function + map data (fixed seed, json control)`
`eclipse v2: disc chunk generator + sector biome source + raised dimension height`

---

## Worker 4 — World stages: ring growth animation, intro fusion, nether stages

**Model:** fable-xhigh

**Prompt:**

Your job: the stage system that grows the disc ring-by-ring at runtime, the intro fusion
animation, and nether stage triggers. Read `docs/ideas/01_world_terrain.md` §B(end)/§C/§D and
`docs/ideas/05_systems.md` §5 (performance rules). W3 already landed `worldgen/`
(`DiscTerrainFunction.stateAt(profile,x,y,z,stage)`, `StageRadii`, `WorldStageAccess` seam).

Tasks:
1. **Config**: add `stages.json` to `core/config/EclipseConfig` (same lazy-default pattern):
   per-dimension stage list `{stage, radius, trigger, structures[], oreBudget{}}` with the
   defaults from `docs/ideas/01_world_terrain.md` §C (overworld stages 1..5 = r 225/300/360/
   420/480, triggers intro_fusion/milestone/milestone/milestone/final_day; nether stages
   1..3 = r 80/120/160, triggers day:2/day:10/day:12).
2. **State**: extend `core/state/EclipseWorldState` (keep old saves loading: default all new
   fields): `worldStageOverworld`, `worldStageNether` (int, default 0), `growthCursor`
   (dimension id string + long column index, default none). Update README Core APIs.
3. **`worldgen/stage/WorldStageService`**: `setStage(MinecraftServer, ResourceKey<Level>, int n, boolean animate)`
   single entry point — persists stage, connects W3's `WorldStageAccess` to EclipseWorldState,
   updates `StageRadii`, kicks `RingGrowthService` when animate, else stamps instantly (still
   tick-budgeted, just no pacing pause), fires structure placement hooks (W5 will subscribe —
   define a simple static listener list now), broadcasts new payload
   `S2CStagePayload(String dim, int stage, int radius, boolean animating)` (register in
   `EclipsePayloads`; client caches into `ClientStateCache` — add fields).
4. **`worldgen/stage/RingGrowthService`** (game-bus, `ServerTickEvent.Post`): precompute the
   annulus column list between old and new radius, ordered radius-then-angle (angular sweep).
   Per tick: drain a **2 ms nanoTime budget** (config `ringBlocksBudgetMs` in `general.json`,
   default 2), writing via `LevelChunkSection.setBlockState(x,y,z,state,false)` per already-
   generated chunk; for never-generated chunks do nothing (chunkgen covers them). After a
   chunk finishes: `Heightmap.primeHeightmaps`, relight via
   `serverLevel.getChunkSource().getLightEngine().relight(Set.of(chunkPos))` (≤4 chunks/tick),
   resend `ClientboundLevelChunkWithLightPacket`. Skip entirely when MSPT > 40. Persist
   `growthCursor` every ~100 columns so a restart resumes mid-animation. ERASE mode: same
   sweep writing air beyond a lowered radius (for stage revert). Garnish the leading edge:
   send `S2CQuasarPayload("eclipse:map_expand_materialize", pos)` for the column being
   materialized, throttled to ~4/second.
5. **`worldgen/stage/FusionSequence`**: special stage-1 sweep — order void columns within
   r=225 by distance to the NEAREST existing disc edge (main r=96 or team discs), so land
   grows outward from the main disc and inward from the player discs simultaneously
   (~60–90 s). Trigger automatically after `StartEventCutscene` completes (hook the point
   where `startEventDone` flips true in `limbo/StartEventCutscene.java`); guard so it runs
   once (stage 0→1).
6. **Triggers**: in `progression/DayScheduler.setDay`, after the day is applied, evaluate
   `stages.json` `day:N` triggers per dimension → `WorldStageService.setStage(..., animate=true)`
   (nether stage 1 on day 2). `milestone` triggers: hook `EclipseWorldState.setAltarLevel`
   callers via a listener in WorldStageService checking on altar change (stage 2 ← altar L2,
   stage 3 ← L3, stage 4 ← L4 by default; read from stages.json `trigger` values like
   `"milestone:2"` — extend the schema accordingly). `final_day` = day 12+.
7. **Commands** (extend `admin/EclipseCommands`, perm 3):
   `/eclipse stage get`, `/eclipse stage set <overworld|nether> <n> [instant|animate]`
   (lower stage → erase mode), `/eclipse stage rebuild <dim> <n>` (re-stamp annulus).

v1 files you may modify: `core/config/EclipseConfig.java`, `core/state/EclipseWorldState.java`,
`admin/EclipseCommands.java`, `progression/DayScheduler.java`, `limbo/StartEventCutscene.java`,
`network/EclipsePayloads.java`, `client/ClientStateCache.java`, `README.md`.

Acceptance: build green. `runServer` fresh world: `/eclipse stage set overworld 2 animate`
visibly logs sweep progress and completes without the watchdog killing the server; check
`spark tps` if available or log MSPT lines — document numbers in your final message;
`/eclipse stage set overworld 1 animate` erases back; restart mid-growth resumes (kill PID
mid-sweep, restart, verify cursor log line). Stop by PID.

Commits:
`eclipse v2: world stage service + stages.json + stage payload`
`eclipse v2: ring growth sweep/erase + intro fusion + day/milestone triggers`

---

## Worker 5 — Structures: stamper, temples/village/stronghold, altar sanctum + protection

**Model:** fable-xhigh

**Prompt:**

Your job: structure placement for the disc world and the altar sanctum at spawn. Read
`docs/ideas/01_world_terrain.md` §E/§F and `docs/ideas/04_content.md` §3. W3/W4 landed
`worldgen/` + `WorldStageService` (a static structure-placement listener list fires when a
stage's terrain sweep completes; `stages.json` lists structure ids per stage).

Tasks:
1. **`worldgen/structure/StructureStamper`**: two mechanisms —
   (a) programmatic vanilla generation replicating `/place structure` internals: resolve a
   `Structure` from `Registries.STRUCTURE`, `Structure.generate(...)` → place `StructureStart`
   pieces — use for `minecraft:village_plains` (flatten a plaza in the plains sector first,
   coarse-dirt pad), `minecraft:desert_pyramid`, `minecraft:jungle_pyramid`,
   `minecraft:stronghold` (guarantees a portal room);
   (b) programmatic set-piece builders (GhostShipBuilder pattern — loops of setBlock) for
   custom landmarks. Positions come from `disc_map.json` landmarks (W3) — desert temple in
   the desert sector inside stage-2 ring, jungle temple stage-3, village stage-4.
   Register a listener with WorldStageService: when stage N terrain completes, place that
   stage's `structures[]` from `stages.json`.
2. **Stronghold finale** (`eclipse:stronghold_emergence`, stage 5): W3 reserved a sealed
   cavity in the mountain core (verify; if not, carve it here deterministically). Sequence:
   quake sounds to all players, a scripted fissure trench opens down the mountainside
   (column edits via the RingGrowthService budget queue), purple beam
   (`S2CQuasarPayload("eclipse:altar_beam", ...)` at the fissure), then stamp
   `minecraft:stronghold` into the cavity with the portal room aligned under the altar axis;
   force portal frames WITHOUT eyes.
3. **Altar sanctum** (`worldgen/structure/AltarSanctumBuilder`, ~25×25 at spawn, build-once
   on `ServerStartedEvent` guarded by new `EclipseWorldState.sanctumBuilt` flag — add field):
   follow `docs/ideas/04_content.md` §3 exactly — 3 concentric polished-blackstone dais steps
   (r=5/3.5/2 at y0/1/2), existing altar block relocated/placed at y3 center, 8 pillars at
   r=9 every 45° (2×2 obsidian base + purpur shafts heights 4/6/7/5/6/4/7/5, three snapped
   with deepslate wall tops), floating purple tinted-glass ring r=4 at y+8 (every other
   block) + crying obsidian ring r=2.5 at y+11 (cardinals), amethyst clusters on 6 dais
   blocks, soul lanterns on chains, sculk veins 20% at dais perimeter, 4×3 purple candles,
   4 cardinal blackstone-slab approach paths, terrain flattened r=12.
   IMPORTANT: coordinate with `GhostShipBuilder`'s spawn-platform logic — sanctum replaces
   the bare spawn platform; make sure the v1 altar block that `ritual/` expects exists at
   the sanctum center (reuse `EclipseBlocks.ALTAR`).
4. **`worldgen/structure/SanctumProtection`** (game-bus): within r=16 of the altar, cancel
   `BlockEvent.BreakEvent`, `BlockEvent.EntityPlaceEvent`, explosion block damage
   (`ExplosionEvent.Detonate` — strip affected blocks in range); ops exempt. Suppress
   non-eclipse hostile spawns via `FinalizeSpawnEvent` in the same radius. Action-bar hint
   on blocked actions (v1 pattern).
5. **Flavor landmarks (budget-permitting, SHOULD tier)**: obsidian watcher statues at former
   team-disc centers placed when fusion completes (subscribe to stage-1 completion);
   sundial plaza around the sanctum (24-block dial, basalt shadow line repositioned by
   `DayScheduler.setDay` — ~40 blocks rewritten). Skip the rest of §F if time-constrained.

v1 files you may modify: `core/state/EclipseWorldState.java`, `limbo/GhostShipBuilder.java`
(spawn platform interplay only), `progression/DayScheduler.java` (sundial hook only),
`README.md`, lang files.

Acceptance: build green. `runServer` fresh world: sanctum builds at spawn (verify via
`/execute if block` probes at dais/pillar offsets logged in your final message); breaking a
dais block as a non-op is cancelled (test with a fake-player is overkill — verify the event
handler registers and document); `/eclipse stage set overworld 2 animate` completes and the
desert pyramid exists (probe blocks); `/eclipse stage set overworld 5 instant` runs the
stronghold emergence without error and `/locate structure minecraft:stronghold` resolves.
Stop by PID.

Commits:
`eclipse v2: structure stamper + staged temples/village/stronghold emergence`
`eclipse v2: altar sanctum builder + spawn protection`

---

## Worker 6 — Cutscene engine "Director": camera paths, freeze, editor, intro rework

**Model:** fable-xhigh

**Prompt:**

Your job: a real cutscene engine — client camera paths with letterbox and skip, a
server-authoritative FreezeService, keyframe editor commands, and the intro rework. Read
`docs/ideas/05_systems.md` §1 and §5 IN FULL (verified event names, the camera-path JSON
schema, watchdog rules). Veil is a required dep (W1): use
`foundry.veil.api.client.util.Easing` for easing; Veil has NO camera API — you build it.

Tasks:
1. **Path model + storage**: `cutscene/CutscenePaths` loads/saves
   `config/eclipse/cutscenes/<id>.json` with EXACTLY the schema from
   `docs/ideas/05_systems.md` §1 (id, allowSkip, interpolation catmullrom|bezier, anchor
   world|player, dimension, letterbox, hideHud, durationTicks, keyframes[t,pos,yaw,pitch,
   roll,fov,easing], events[t,type,id]). Ship defaults written on first run: `intro_submerge`
   (limbo, ship submerge flyaround), `intro_rise` (overworld, per §1 example JSON, anchor
   player), `unlock_ring` (orbital shot at a ring edge, anchor world, parameterized center
   via a `"params"` passthrough), `finale_return` (reverse of intro, used by W12). Per-path
   `enabled` flag + persisted disabled set in `EclipseWorldState` (add `disabledCutscenes`
   Set<String> field).
2. **Server `cutscene/CutsceneService`**: `play(id, Collection<ServerPlayer>)` → per player:
   `FreezeService.freeze(player, durationTicks + 100)`, send `S2CCutscenePlayPayload(id, allowSkip)`.
   Library sync: `S2CCutsceneLibraryPayload` (all path JSON as strings) at
   `PlayerLoggedInEvent` + on `/eclipse cutscene reloadpaths`. Client ACKs via
   `C2SCutsceneStatePayload(enum STARTED|FINISHED|SKIP_REQUEST|SKIPPED)`; server validates
   SKIP_REQUEST against `allowSkip` && not-disabled. Disabled cutscene → skip straight to
   end-state callbacks (never softlock). `abort(players)` command path.
3. **`cutscene/FreezeService`** (server, game-bus): new TRANSIENT (non-persisted) attachment
   `CUTSCENE_LOCK` in `registry/EclipseAttachments` (no `.copyOnDeath()`, not serialized —
   restart always unfreezes). `PlayerTickEvent.Pre`: rubber-band (`connection.teleport` if
   moved >0.1), zero `setDeltaMovement`; cancel `PlayerInteractEvent`s; invuln = cancel
   `LivingIncomingDamageEvent` + `LivingKnockBackEvent` while locked. NEVER flip
   `abilities.invulnerable`. Mandatory TTL per freeze; watchdog releases at TTL, on death,
   dimension change (except a scripted-intro flag), logout; clear stale locks at login.
   Public API: `freeze(player, ttlTicks)`, `unfreeze(player)`, `isFrozen(player)` — W7
   border physics and W14 commands will call this.
4. **Client `cutscene/client/`**: `CameraDirector` singleton — active path, start nanos;
   position via Catmull-Rom (default) or cubic bezier; rotation = quaternion slerp of
   yaw/pitch/roll; per-keyframe easing mapped to Veil `Easing`. Position override: mixin
   `@Inject(method="setup", at=@At("TAIL"))` into `net.minecraft.client.Camera` calling
   `setPosition`/`setRotation` — widen those protected methods via
   `META-INF/accesstransformer.cfg` (v1 file, append). Roll/rotation fallback via
   `ViewportEvent.ComputeCameraAngles`; FOV via `ViewportEvent.ComputeFov`. During flight
   `options.setCameraType(CameraType.THIRD_PERSON_BACK)` so the own body renders; restore
   after. Register the mixin in `eclipse.client.mixins.json`.
   `CutsceneInput`: `MovementInputUpdateEvent` zero-all, cancel `InputEvent.MouseButton.Pre`
   + `InteractionKeyMappingTriggered`; ESC/Space → SKIP_REQUEST payload.
   `LetterboxLayer`: GUI layer (register in `EclipseGuiLayers`) easing two black bars in/out;
   suppress other HUD via `RenderGuiLayerEvent.Pre` cancel while active (do NOT cancel our
   letterbox or W2's burst if mid-death — guard by layer id).
   Missing path client-side → ACK FINISHED instantly.
5. **Intro rework** (`limbo/StartEventCutscene.java`): keep the server timeline (TILT t=0,
   SUBMERGE t=100, teleport t=140, EMERGE t=160). At TILT also
   `CutsceneService.play("intro_submerge", allPlayers)`; on the dimension-change teleport
   chain `intro_rise` (anchor player). The fusion sequence (W4) triggers after — verify the
   hook still fires. Keep `S2CCutscenePayload` phases for the v1 `WaveOverlay` (regression).
6. **Unlock-animation integration**: subscribe to `WorldStageService` growth start/end —
   during an ANIMATED stage growth, freeze+invuln all players (TTL = generous estimate) and
   play `unlock_ring` anchored at the nearest ring edge; on completion unfreeze. Global
   dev toggle `cutscenes.freezeDuringUnlocks` (general.json, default true).
7. **Commands** (extend `admin/EclipseCommands`):
   `/eclipse cutscene play <id> [players] | abort [players] | list | enable <id> | disable <id> |
   skip allow|deny <id> | preview <id>` (operator only, no freeze) `| reloadpaths |
   edit <id> addkeyframe [t]` (captures eye pos/yaw/pitch; `set roll|fov <v>` on last
   keyframe; `removekeyframe <i>`; `export <id>` prints JSON). Editor writes through
   `CutscenePaths` and re-syncs the library.

v1 files you may modify: `registry/EclipseAttachments.java`, `admin/EclipseCommands.java`,
`limbo/StartEventCutscene.java`, `client/EclipseGuiLayers.java`,
`network/EclipsePayloads.java`, `eclipse.client.mixins.json`,
`META-INF/accesstransformer.cfg`, `core/config/EclipseConfig.java` (general.json fields),
`core/state/EclipseWorldState.java` (disabledCutscenes), `README.md`.

Acceptance: build green (AT validation is strict — `validateAccessTransformers=true`).
`runServer`: `/eclipse cutscene list` shows 4 defaults; `/eclipse cutscene play intro_rise`
against zero players errors gracefully; `/eclipse freeze` is W14 — but
`FreezeService.freeze/unfreeze` must be public now. Client-side visuals verified in W16.
Stop by PID.

Commits:
`eclipse v2: freeze service + cutscene payloads + path library`
`eclipse v2: client camera director (catmull-rom, roll, fov, letterbox, skip) + intro rework`
`eclipse v2: cutscene editor commands + unlock-growth freeze integration`

---

## Worker 7 — Circular soft worldborder + glitch FX

**Model:** fable-xhigh

**Prompt:**

Your job: replace the square vanilla border experience with a circular soft border that is
invisible until close, glitches purple, and pushes back robustly. Read
`docs/ideas/05_systems.md` §2 IN FULL and `docs/ideas/02_veil_vfx.md` §B.4/§E.6.
Existing pieces: `progression/BorderController` (vanilla border wrapper),
`WorldStageService` + `S2CStagePayload` (W4), `FreezeService` (W6), Quasar `border_glitch`
emitter + `VeilPostController` (W1).

Tasks:
1. **`border/SoftBorder`** (server): state = center (world spawn) + radius, persisted in
   `EclipseWorldState` (add `borderCenterX/Z`, `softBorderRadius`, defaults spawn/1000).
   Radius follows the world stage: on stage commit, `radius = stageRadius + 12` (lerp over
   the growth animation duration — tick-interpolate). `S2CBorderPayload(centerX, centerZ,
   radius, fxRange)` at login + on change. `/eclipse border ring set <radius> [seconds]` and
   `/eclipse border fx range <blocks>` commands (extend the existing `/eclipse border` node).
2. **Physics** (game-bus, `PlayerTickEvent.Post`, cheap d² math; skip frozen players):
   - `d > R−16`: mark near-border (drives client FX flag — include in payload or derive
     client-side from position; derive client-side, no extra traffic).
   - `d > R`: horizontal impulse `v = normalize(center−pos) * min(1.2, 0.25*(d−R)+0.4)` +
     0.3 Y; `player.setDeltaMovement(v); player.hurtMarked = true;` (pattern proven in
     `StartEventCutscene.risePlayerAt`). Elytra: `player.stopFallFlying()` first. Play a
     glitch sound + trigger client bounce FX.
   - `d > R+3`: teleport fallback to the clamped point at R−2 (raycast ground for a safe Y).
   - **Vehicles**: operate on `player.getRootVehicle()`; impulse the vehicle; if the same
     vehicle re-violates 3× within 40t → eject + teleport the player alone. Scan vehicles in
     `EntityTickEvent.Post` ONLY when they carry player passengers.
   - **Pearls/chorus**: `EntityTeleportEvent.EnderPearl`/`.ChorusFruit` — clamp
     `setTargetX/Z` into R−2 (do NOT cancel — that eats pearls).
   - Sable sub-levels (Aeronautics airships) are NOT vanilla vehicles: if a player's
     position crosses R while `getRootVehicle() == player` but they're moving impossibly
     (no ground), still teleport the PLAYER out — the generic `d > R+3` fallback covers it;
     document this limit in README.
   - Log repeat violators (>5 pushbacks/min) via `EclipseMod.LOGGER.warn` (AntiCheat channel).
3. **Vanilla failsafe**: keep `BorderController` but repoint it — vanilla border always sits
   at `softRadius + 48`, warning 0, damage 0. Remove the days.json borderSize driving path
   from `progression/DayScheduler` (field stays parsed; log a one-time deprecation warning).
   Hide vanilla border visuals: client mixin cancelling `LevelRenderer#renderWorldBorder`
   (head inject, cancellable return) — add to `eclipse.client.mixins.json`.
4. **Client FX** (`border/client/BorderFxRenderer`): invisible until
   `distToCircle < fxRange` (default 8, server-configurable via payload).
   (a) Geometry: `RenderLevelStageEvent` AFTER_PARTICLES — curved strip of quads covering
   ±25° of arc nearest the player, tessellated every 2 blocks, height ±12 around player Y,
   scrolling glitch texture (`assets/eclipse/textures/environment/border_glitch.png` —
   procedural noise PNG via `scripts/placeholder_gen/`), additive blend, alpha =
   `1 − dist/fxRange` × per-quad noise flicker. Budget: ≤200 quads. If sorting artifacts
   appear under Sodium, switch stage to AFTER_TRANSLUCENT_BLOCKS (leave a comment).
   (b) Particles: spawn Quasar `border_glitch` emitters along the visible arc, ≤30/s.
   (c) Veil post: chromatic-aberration pipeline `pinwheel/post/border_glitch.json` with a
   `Proximity` uniform — via `VeilPostController`, auto-gated off when a shaderpack is
   active or `veilPostFx=false`.
5. Client caches border params from `S2CBorderPayload` in `ClientStateCache`.

v1 files you may modify: `core/state/EclipseWorldState.java`,
`progression/BorderController.java`, `progression/DayScheduler.java`,
`admin/EclipseCommands.java`, `network/EclipsePayloads.java`, `client/ClientStateCache.java`,
`eclipse.client.mixins.json`, `README.md`.

Acceptance: build green. `runServer`: `/eclipse border ring set 100` then `/tp @s 120 ~ 0`
— server log shows pushback/teleport engagement (add a debug log line, keep at debug level);
pearl-throw clamp compiles against the right events; vanilla `/worldborder get` reports
soft+48. Visual FX verified in W16. Stop by PID.

Commits:
`eclipse v2: circular soft border physics (vehicles, elytra, pearls) + vanilla failsafe`
`eclipse v2: border glitch fx (geometry strip, quasar arcs, veil aberration) + hide vanilla border`

---

## Worker 8 — HUD suite: skinned bossbars, sidebar panel, typewriter announcements, timeline sync

**Model:** fable-xhigh

**Prompt:**

Your job: the announcement + HUD skin layer. Read `docs/ideas/03_ui_ux.md` §A ("Boss bar",
"Scoreboard"), §D and §E IN FULL — the event names and math there are verified.
Existing pieces: `EclipseClientConfig` toggles (W1: `showBossbarSkin`, `showSidebar`),
`ClientStateCache` (lives/day/altarLevel/goals + stage/border from W4/W7), revive-ritual
bossbar (`ritual/ReviveRitual`), day bell in `progression/DayScheduler`.

Tasks:
1. **Bossbar skin** (`client/hud/BossbarSkin`): subscribe
   `CustomizeGuiOverlayEvent.BossEventProgress` (game bus, cancellable, per bar). Match OUR
   bars only: maintain a client set of skinned bar UUIDs — add payload
   `S2CBossbarStylePayload(UUID id, String theme)` (playToClient) that the server sends when
   it creates a themed bar; themes: `day`, `goal`, `boss`. For matched bars: cancel, call
   `setIncrement(getIncrement() + 10)`, draw at `(getX(), getY())`: 256×32 frame texture
   drawn at 182×14+ logical px, lerped fill (approach real progress at 0.05/frame),
   scrolling energy overlay (UV offset by `Util.getMillis()`), end-cap glow flash on
   progress change. Textures per theme:
   `assets/eclipse/textures/gui/bossbar/{day,goal,boss}_frame.png` (512×64),
   `fill.png` (512×32), `scroll.png` (256×32), `glow.png` (64×64) — programmer-art via
   `scripts/placeholder_gen/`. `showBossbarSkin=false` → render a minimal 4px strip
   (NEVER fully hide revive countdowns). Unmatched bars → vanilla (don't cancel).
   Tag the existing revive-ritual bossbar with theme `goal` (edit `ritual/ReviveRitual`
   where the ServerBossEvent is created).
2. **Sidebar panel** (`client/hud/SidebarPanel`): do NOT touch vanilla scoreboard data.
   Register a GUI layer (in `client/EclipseGuiLayers`); if any vanilla sidebar objective is
   set, cancel `RenderGuiLayerEvent.Pre` for `VanillaGuiLayers.SCOREBOARD_SIDEBAR` while
   ours is on. Right-anchored panel 110px wide × rows*12px: rows = hearts (icon + n),
   day, altar level, online count, personal goal ticks. Goal data: new
   `S2CGoalProgressPayload(List<String> goalLines, List<Boolean> done)` — server tracks
   per-player goal completion in a new `economy/GoalTracker`-style holder OR, if goal
   completion tracking doesn't exist server-side yet, send all-false and leave a TODO hook
   (W13 wires real goal ticking). 60% black rounded backdrop, 12×12 icons
   (`assets/eclipse/textures/gui/sidebar/` — placeholder art), slide-in animation on change.
   Honor `showSidebar` live.
3. **Announcements** (`client/hud/AnnouncementOverlay` + `TypewriterLine`): one payload
   `S2CAnnouncePayload(String titleKey, String subtitleKey, String style)` (styles:
   `day`, `unlock`, `goal`, `boss`). Client: (a) typewriter line above the hotbar — types 1
   char/tick with a tick sound every 2 chars (`eclipse:ui.typewriter`, register + placeholder
   ogg), then posts the completed Component to chat once; (b) bossbar sweep — client-only
   temp bar using the `boss`-theme skin: fill sweeps 0→1 over 30t with a bright leading
   edge, holds 60t showing the title (e.g. "DAY 4 — THE VEIL THINS"), fades.
4. **Server wiring** (`cutscene`-free, plain events): fire announce on — day change
   (`progression/DayScheduler.setDay`, replace/augment the bell), unlock-key additions
   (compare `UnlockState.unlockedKeys` before/after day+altar changes), stage growth
   completion (subscribe to `WorldStageService`), milestone level-ups. Titles from lang keys
   (`announce.eclipse.day.N`, etc. — add en_us + de_de).
5. **Timeline sync** (consumed by W9's handbook): `record TimelineEntry(int id, int unlockDay,
   String titleKey, ResourceLocation icon, boolean hidden, boolean reached)`;
   `S2CTimelinePayload(List<TimelineEntry>)` on login + day change. Server builds entries
   from `days.json` + milestones; FUTURE entries are sent WITHOUT title/icon (server-side
   anonymization — no datamining). Cache in `ClientStateCache.timeline`.

v1 files you may modify: `progression/DayScheduler.java`, `ritual/ReviveRitual.java`,
`client/EclipseGuiLayers.java`, `client/ClientStateCache.java`,
`network/EclipsePayloads.java`, `registry/EclipseSounds.java`, `assets/eclipse/sounds.json`,
lang files, `README.md`.

Acceptance: build green. `runServer`: `/eclipse day set 4` log shows announce payloads sent;
`/eclipse altar set 2` triggers unlock announce; revive-ritual bar carries a style payload
(code-verify). Visuals in W16. Stop by PID.

Commits:
`eclipse v2: skinned bossbars (3 themes) + custom sidebar panel + client toggles`
`eclipse v2: typewriter/sweep announcements + timeline sync payloads`

---

## Worker 9 — Handbook 2.0: six-tab codex, cursors, UI sounds, page animations

**Model:** fable-xhigh

**Prompt:**

Your job: replace the small artifact popup with "The Ledger of the Drowned" — a 6-tab
full-screen interactive codex. Read `docs/ideas/03_ui_ux.md` §A ("Cursors", "Hover sounds",
"Hi-res art", "GUI shaders"), §B and §F IN FULL. Existing pieces: `client/ArtifactScreen`
(opened via `S2COpenArtifactPayload` or the `key.eclipse.menu` keybind J — keep BOTH open
paths, they now open the handbook), `client/RulesScreen` (absorb as a tab),
`ClientStateCache` (lives/day/altar/goals/stage/border/timeline — W4/W7/W8 filled these).

Tasks:
1. **`client/handbook/HandbookScreen`**: full-bleed dark vignette over the world; centered
   book 90%w × 85%h — ALL coordinates derived from width/height percentages (no fixed
   176×166 box). Left page = nav + hero art, right page = content. Six parchment tab
   tongues on the far-left spine with icons; active tab slides out 6px + glows.
   Bottom: page dots + key hint. Tabs (each its own class under `client/handbook/tabs/`):
   - **Status**: big day counter, live heart row (reads `ClientStateCache.lives`), altar
     progress ring (pulses on level-up), today's goals with animated tick draw-in, online
     count (from tab-list size — it's hidden visually but the client still has player info).
   - **Timeline**: horizontal scrollable spine from `ClientStateCache.timeline` (W8).
     Unlocked entries: icon + title. Hidden/future: "???" silhouette + glitch text
     (random chars re-rolled every 3 ticks — `client/handbook/GlitchText` helper).
     Inertial drag scroll; grab cursor while dragging.
   - **Rules**: absorb `RulesScreen` content as a scrollable parchment (keep the
     `RulesScreen` class delegating to the handbook tab or delete it and update its two
     call sites — your choice, document it).
   - **Rewards**: milestone costs/rewards from a new `S2CMilestonesPayload` (add it —
     server sends `EclipseConfig.milestones()` at login/reload; costs rendered with
     `guiGraphics.renderItem` item icons).
   - **Bestiary**: cards for The Other, Gazer, Umbral Stalker, Deckhand, Herald, Ferryman —
     revealed once encountered/unlocked by day (client-side rule: day >= mob's intro day);
     locked = silhouette + glitch text. Data table hardcoded client-side.
   - **Map**: stylized concentric ring diagram from stage/border cache — rings per stage
     radius, current soft-border circle, spawn dot, discovered landmark dots (stage-gated).
2. **Animations** (all honoring `reducedFx`): open = book unfold (scaleY 0.9→1 + alpha,
   8t ease-out cubic via pose stack scaling around center); tab switch = page-turn (old page
   skews/compresses 6t + `ui.page_turn` sound); parallax: background layer shifts 8px
   opposite the mouse, mid layer 4px; hover glow fade-in 4t on every interactive element.
3. **Cursors** (`client/handbook/CursorManager`): GLFW — handle from
   `Minecraft.getInstance().getWindow().getWindow()`. Themed cursors from PNGs
   (`assets/eclipse/textures/gui/cursor/{arrow,hand,grab}.png`, 32×32, hotspots
   (0,0)/(8,0)/(16,16)) loaded via STBImage/NativeImage → `GLFW.glfwCreateCursor`; fallback
   `glfwCreateStandardCursor(GLFW_POINTING_HAND_CURSOR)` guarded (0 → skip). Apply with
   `glfwSetCursor`; **ALWAYS reset via `glfwSetCursor(handle, MemoryUtil.NULL)` in
   `Screen#removed()`**; render-thread only; cache pointers; destroy + recreate on resource
   reload; swap only on hover-state change; `customCursor=false` config disables entirely.
4. **UI sounds**: register `eclipse:ui.hover`, `ui.page_turn`, `ui.tab`, `ui.unlock_sting`
   in `EclipseSounds` + `sounds.json` + placeholder oggs. Hover = edge-detect `wasHovered` →
   `SoundManager.play(SimpleSoundInstance.forUI(...))`. Honor `uiSounds` config.
5. **Art**: create placeholder programmer-art at the EXACT paths/dimensions listed in
   `docs/ASSET_MANIFEST_V2.md` Batch A (book spread 2048×1280, parchment tile 1024×1024,
   6 tab icons 64×64, hero art 1024×768 ×6, timeline nodes 96×96 ×3, divider 512×64,
   cursor set) via `scripts/placeholder_gen/`. Blit with explicit full texture dims so
   real hi-res art drops in unchanged; pixel-snap `Math.round(v * guiScale) / guiScale`.
6. **Opener**: `ArtifactScreen` becomes a thin alias — `S2COpenArtifactPayload` handler and
   the J keybind now open `HandbookScreen` (edit `client/ArtifactScreenOpener`,
   `client/ArtifactKeyHandler`). Keep `ArtifactScreen.java` deleted or as a deprecated
   stub — update all references.

v1 files you may modify: `client/ArtifactScreen.java`, `client/ArtifactScreenOpener.java`,
`client/ArtifactKeyHandler.java`, `client/RulesScreen.java`, `registry/EclipseSounds.java`,
`network/EclipsePayloads.java` (S2CMilestonesPayload), `assets/eclipse/sounds.json`,
lang files (many new keys, en_us + de_de), `README.md`.
Do NOT add any global `/` key handler (command-palette-style) — the existing keybind is J.

Acceptance: build green. `runServer` boots (payload registration). Client visual pass in
W16; your own gate: launch `./gradlew runClient` if the environment allows, open a
singleplayer world, press J, screenshot each tab (attach to your final message), verify no
crash on open/close ×5 (cursor lifecycle) and on F3+T resource reload with the book open.
Kill client by PID after.

Commits:
`eclipse v2: handbook 2.0 shell + six tabs + timeline/bestiary/map`
`eclipse v2: handbook cursors, ui sounds, page-turn + parallax animations`

---

## Worker 10 — Custom mobs: The Other, Gazer, Umbral Stalker, Deckhand (+Sunmote), spawner + night events

**Model:** fable-xhigh

**Prompt:**

Your job: four (optionally five) custom mobs with hand-coded cube models, procedural
animations, programmer-art textures, and a day/event-keyed spawner. Read
`docs/ideas/04_content.md` §1 and the "Shared infra" note IN FULL — cube sizes, pivots,
AI goals, drops, and sounds are all specified there. Quasar particles ARE available
(they ship inside Veil — use `veilfx.QuasarSpawner` / `S2CQuasarPayload` from W1).

Tasks:
1. **Shared infra**: `entity/EclipseEntities` — `DeferredRegister<EntityType<?>>` (wire in
   `EclipseMod`), `EntityAttributeCreationEvent` for attributes,
   `EntityRenderersEvent.RegisterLayerDefinitions` + `RegisterRenderers` in a new
   `client/entity/EclipseEntityRenderers` (mod-bus, client). Models are authored AS CODE
   with `MeshDefinition`/`PartDefinition`/`CubeListBuilder` — simple boxy UV layouts,
   UV-planned so a texture artist can paint them (see task 5).
2. **Mobs** (specs verbatim from `docs/ideas/04_content.md` §1.1–§1.5):
   - `TheOtherEntity` (§1.1): vanilla `HumanoidModel` geometry reuse; texture = copy of
     `assets/eclipse/textures/entity/uniform_skin.png` with pure-black eyes + faint purple
     face seam (generate programmatically from the existing PNG). AI per spec (FloatGoal,
     stroll, LookAtPlayer 32f, custom `MimicWalkGoal` — paths to nearest player at 0.42,
     stops at 5 blocks and stares —, MeleeAttackGoal(1.2,false) gated to ≤3 blocks or
     retaliation, HurtByTarget; random `setSprinting` mannerisms; 180° head snap in 2t on
     aggro). Spawns ONLY during Pale Night events, 2–3 per event, ≥24 blocks from players,
     surface; despawns at dawn with SOUL_ESCAPE + `arm_wisps` Quasar burst. Drops 1–2
     `eclipse:umbral_shard` (NEW item — register in `EclipseItems`, 16×16 placeholder icon,
     lang). Silent idle; hurt = PLAYER_HURT pitch 0.8; death = WARDEN_SONIC_CHARGE vol 0.3.
   - `GazerEntity` (§1.2): 6-cube model per spec (floating root, cloak 10×18×6, hood
     8×8×8, emissive face inset via `RenderType.eyes` layer, 2 tatters). No movement goals;
     `VanishWhenSeenGoal` (player look-vector dot ≥0.985 for 40t → despawn + wisp puff +
     mood sound), `RelocateGoal` (every 200–400t teleport 20–40 blocks into peripheral FOV,
     dot 0.3–0.8). `hurt()` → vanish (unkillable). Whisper loop sound
     `eclipse:ambient.gazer_whisper` ≤12 blocks (register + placeholder ogg). Spawns
     overworld nights day 3+, 1 per ~4 players, 1 guaranteed near the altar during
     sacrifices (hook `ritual/AltarBlockEntity` deposit path with a spawn call).
   - `UmbralStalkerEntity` (§1.3): 11-cube quadruped per spec (body/head/jaw shards/4 legs/
     3 spine shards with the exact sizes+pivots given). AI: FloatGoal, LeapAtTarget(0.4),
     MeleeAttack(1.3,true), stroll, RandomLook, HurtByTarget().setAlertOthers(),
     NearestAttackableTarget<Player>(true). 20 HP / 4 dmg / speed 0.32 / follow 40. Packs
     3–4 at night day 5+, doubled on Umbral Nights, flees+despawns at dawn. Drops 0–2
     umbral_shard, 20% `heart_fragment` (item exists). Quadruped leg swing
     `cos(limbSwing*0.66+phase)*1.2*limbSwingAmount`, shard pulse-breathing, head lowers
     0.3 with target.
   - `DeckhandEntity` (§1.4): 7-cube hunched rower per spec; only LookAtPlayerGoal(48f);
     invulnerable, no push, limbo only. 6–8 seated at the ghost-ship oar benches — spawn
     from `limbo/GhostShipBuilder` (once, guarded like the oars; persist UUIDs in
     `EclipseWorldState.getOarEntities()`-style list — add a `deckhandEntities` list).
     Rowing anim synced to the same 30-tick cadence as `limbo/OarAnimator`
     (`arm.xRot = −1.2 + sin(age*0.08)*0.35`).
   - `SunmoteEntity` (§1.5) ONLY if you're on budget: 2-cube fullbright wisp orbiting the
     altar (radius 6+altarLevel), maintained by `ritual/AltarBlockEntity`; killable → 1
     glowstone dust; AMETHYST_BLOCK_CHIME every ~200t. Skip cleanly if short on time.
3. **`entity/EclipseSpawner`** (game-bus, ServerTickEvent.Post, ~100t cadence): all spawn
   rules above keyed off `DayScheduler.getDay` + night-time check + the night-event state.
   **Night events**: add to `EclipseWorldState`: `activeNightEvent` (none|pale|umbral +
   day stamp). Scheduler: on nightfall of day 4+ roll Pale Night (first one guaranteed
   day 4), day 6/10 Umbral Night (per the §6 arc); announce via W8's
   `S2CAnnouncePayload` style `unlock`. Expose `/eclipse event set <pale|umbral|none>`
   admin command for testing (extend EclipseCommands).
4. **Anonymity regression guard**: The Other must render with the uniform skin pipeline
   untouched — it's a NEW entity type with its own renderer, so the
   `AbstractClientPlayerMixin` doesn't apply; verify your renderer uses your texture copy.
5. **UV docs + placeholder textures**: for EVERY new model commit
   (a) a programmer-art texture at `assets/eclipse/textures/entity/<mob>.png` painted
   region-by-region via a `scripts/placeholder_gen/` generator (distinct flat colors per
   body part + 1px seam lines), and (b) `docs/uv/<mob>.md` describing the UV layout:
   texture size, and for each cube — name, UV origin, box dims, pixel rects of each face.
   The orchestrator will image-gen final art to match; your art must make the game fully
   playable NOW.
6. Lang entries (en_us + de_de) for entity names; spawn eggs NOT needed (event mod).

v1 files you may modify: `EclipseMod.java`, `registry/EclipseItems.java`,
`registry/EclipseSounds.java`, `core/state/EclipseWorldState.java`,
`limbo/GhostShipBuilder.java`, `ritual/AltarBlockEntity.java`,
`admin/EclipseCommands.java`, `assets/eclipse/sounds.json`, lang files, `README.md`.

Acceptance: build green. `runServer`: `/summon eclipse:gazer`, `/summon eclipse:umbral_stalker`,
`/summon eclipse:the_other` all succeed; `/eclipse event set pale` flips state (check
`/eclipse status` — add night event to its dump); deckhands appear on the limbo ship
(`/eclipse tp_limbo`, then `/execute if entity @e[type=eclipse:deckhand]`). Stop by PID.

Commits:
`eclipse v2: entity infra + umbral shard item + the other/gazer/stalker/deckhand models+ai`
`eclipse v2: day/event spawner + pale/umbral nights + uv docs + placeholder skins`

---

## Worker 11 — Boss 1: The Herald of the Eclipse (day 7)

**Model:** fable-xhigh

**Prompt:**

Your job: the day-7 boss. Read `docs/ideas/04_content.md` §2.1 IN FULL — model cubes, phase
mechanics, scaling, and drops are specified there. Existing pieces: sanctum arena with 8
pillars (W5 — pillars are the P2 line-of-sight cover), `entity/` infra + Umbral Stalkers
(W10), skinned bossbar theme `boss` via `S2CBossbarStylePayload` (W8), Quasar payloads (W1).

Tasks:
1. **Summon item**: `eclipse:heralds_lure` (recipe: 4 umbral shards + 1 heart_fragment —
   add `data/eclipse/recipe/heralds_lure.json`; also craftable listing in the rewards tab
   comes free from milestones — not needed here). Depositing the lure at the altar
   (`ritual/AltarBlockEntity` interaction path, sneak-use with the item) after dusk spawns
   the Herald 12 blocks above the sanctum center.
2. **`entity/boss/HeraldEntity`** (~27 cubes per spec: core 12×12×12 floating pivot
   (0,40,0), emissive innerEye 6×6×6 via `RenderType.eyes`, 8 corona shard wedges 2×6×2 on
   a rotating ring bone r=14px, 4 tentacle chains × 4 cubes 2×6×2). Procedural anims per
   spec: ring `yRot = age*0.05`, shard bob `sin(age*0.1 + i*π/4)*2px`, tentacle segment k
   `xRot = sin(age*0.09 + k*0.6)*0.25`, core bob; P3 speeds ×2 + shards tilt out
   (zRot lerp 0.6). Commit UV doc `docs/uv/herald.md` + programmer-art texture (128×128)
   per the W10 pattern (scripts/placeholder_gen generator).
3. **Fight** (300 HP base; `ServerBossEvent` PURPLE, NOTCHED_6, styled `boss` via W8's
   payload; arena lock = particle wall r=15 — pushback like the soft border, reuse the
   impulse math from `border/SoftBorder`):
   - P1 Volley (100–66%): hover 8–12 above dais, strafe; every 60t telegraph (shards glow +
     BEACON_POWER_SELECT, 20t) then 3 homing `HeraldShardProjectile`s (ShulkerBullet-style,
     4 dmg, shootable down); every 200t summon 2 Umbral Stalkers (cap 4).
   - P2 Gaze (66–33%): guardian-style beam — lock one player (ONLY they hear
     WARDEN_HEARTBEAT via `playNotifySound`), 40t charge with a visible wisp beam
     (Quasar `altar_beam` re-aimed or a dedicated emitter), 8 dmg + Darkness 5 s unless LOS
     is broken behind a pillar. Volley cadence 90t.
   - P3 Collapse (33–0%): descend to y+3, pull players 0.08/t toward center, expanding
     damage rings every 80t (radius +0.4/t, jumpable; SOUL_FIRE_FLAME floor markers);
     corona shards detach as HP drops and crash as AoE (`boss_slam` Quasar burst via
     S2CQuasarPayload).
   - Scaling: HP = 300*(1+0.35*(n−1)) living players at summon; stalker cap 2+n;
     telegraph −2t per player (floor 12).
4. **Drops/state**: on death drop 1 `eclipse:herald_core` (new item, 16×16 placeholder) +
   3 umbral shards at EACH participant's feet; set new `EclipseWorldState.heraldDefeated`
   flag; fire W8 announce (style `boss`); unlock hook: day-7 plan lists `enchanting` as an
   on-kill unlock — implement as: if `days.json` day 7 contains unlock `enchanting`, gate
   its effect behind heraldDefeated by ADDING a derived unlock key `herald_slain` that
   `UnlockState` unions in when the flag is set, and let W13's config move `enchanting`
   to a milestone/flag — for now just set the flag + announce (W13 finalizes the arc).
5. Boss sounds: placeholder oggs `eclipse:boss.herald_ambient`, `boss.herald_telegraph`
   (+ registrations). Bossbar name: "☀ The Herald" (lang key).
6. Admin: `/eclipse boss herald summon|kill` for testing (extend EclipseCommands).

v1 files you may modify: `ritual/AltarBlockEntity.java`, `registry/EclipseItems.java`,
`registry/EclipseSounds.java`, `core/state/EclipseWorldState.java`,
`progression/UnlockState.java` (herald_slain union), `admin/EclipseCommands.java`,
`network/EclipsePayloads.java` (only if a new payload is needed), lang, sounds.json,
`README.md`.

Acceptance: build green. `runServer`: `/eclipse boss herald summon` spawns it with a purple
notched bossbar; log the phase transitions by damaging via
`/damage @e[type=eclipse:herald,limit=1] 110` steps; verify P2/P3 log lines; `/eclipse boss
herald kill` drops herald_core (`/execute if entity @e[type=item]`-style probe or log).
Stop by PID.

Commits:
`eclipse v2: herald boss model + shard projectile + uv doc`
`eclipse v2: herald three-phase fight, arena lock, drops + summon ritual`

---

## Worker 12 — Boss 2: The Ferryman (day 14 finale) + mass-revive ending

**Model:** fable-xhigh

**Prompt:**

Your job: the day-14 finale boss on the limbo ghost ship, including the ghost-participation
mechanic and the mass-revive win cinematic. Read `docs/ideas/04_content.md` §2.2 IN FULL.
Existing pieces: ghost ship + oars (`limbo/GhostShipBuilder`, `OarAnimator`), Deckhands
(W10), `BanService`/`ReviveRitual` (v1), `CutsceneService` + `finale_return` path (W6),
bossbar theme (W8), `FreezeService` (W6).

Tasks:
1. **Finale ritual**: sneak-depositing a DRAGON_EGG at the altar on day 14 after dusk starts
   the finale (hook `ritual/AltarBlockEntity` like the Herald lure): all LIVING players are
   teleported to the ghost-ship deck (ghosts/banned are already in limbo — pull any
   stragglers to the deck too), a short `CutsceneService.play("intro_submerge", ...)` reuse
   or a new `finale_arrival` path introduces the fight, then `FerrymanEntity` spawns at the
   stern.
2. **`entity/boss/FerrymanEntity`** (~18 cubes, 3.5 blocks tall per spec: floating robe
   10×26×8 + 4 hanging strips, skull 7×7×7 under hood 9×9×9, arms 3×20×3, two-handed oar
   2×2×36, lantern 4×5×4 on a 3-segment chain with drag-lag swing). UV doc
   `docs/uv/ferryman.md` + 128×128 programmer-art texture (W10 pattern).
3. **Fight** (400 HP base, `ServerBossEvent` themed `boss`; bar color WHITE→PURPLE→RED by
   phase — recreate the ServerBossEvent or setColor per phase):
   - P1 Oar: melee stalker; telegraphed 180° sweep (raise oar 25t, TRIDENT_RIPTIDE_3 sound,
     10 dmg + heavy knockback — overboard players take void-cold water DoT until they climb
     back via the boarding nets; implement DoT as a limbo-water damage tick during the
     fight). "Gunwale slam": jump-slam with a client shake — new tiny
     `S2CShakePayload(float strength, int ticks)` payload; client shakes the camera when no
     cutscene is active (`CameraDirector` hook or view-bobbing offset in
     `ViewportEvent.ComputeCameraAngles`).
   - P2 Crew (≤66%): Ferryman kneels invulnerable at the stern; 6–8 Deckhands rise hostile
     (flip a hostility flag on W10's DeckhandEntity — add goals activated by the flag);
     4 deck lanterns extinguish (real lantern blocks on the ship — place them in
     GhostShipBuilder if absent). LIVING players kill Deckhands; GHOSTS re-light lanterns
     (right-click 3 s channel — ghosts are the `eclipse_ghosts` team, adventure mode; add a
     ghost-only interaction handler). 4 lanterns lit → phase ends.
   - P3 The Toll (≤33%): plants oar; ship sinks — water level rises 1 layer per 30 s (soft
     enrage; block edits budget-friendly, reuse RingGrowthService-style queue or direct
     small-volume setBlock). Alternates sweeps with "Lantern Gaze": lowest-hearts player is
     marked (vignette overlay only they see — reuse `S2CAnnouncePayload` style or a tiny
     flag payload; simplest: send S2CShakePayload variant with a `marked` flag → client
     purple vignette 15 s) and hunted 15 s.
   - Scaling: HP ×(1+0.4*(n−1)) living; lanterns required = min(4, ghosts+2); sink pauses
     if ≤3 players alive.
4. **Endings**: on Ferryman death — drop `eclipse:ferryman_toll` (new item), set
   `ferrymanDefeated`, then MASS REVIVE: for every banned player run the
   `BanService.unban` path (batch, staggered 10t apart), play
   `CutsceneService.play("finale_return", everyone)` (reverse intro: ship rises, players
   emerge at overworld spawn — reuse `StartEventCutscene.risePlayerAt`-style placement),
   announce style `boss` ("THE CROSSING ENDS"). Wipe (all living dead during the fight) =
   Eclipse victory ending: announce + keep everyone ghosts (no revive).
5. Admin: `/eclipse boss ferryman summon|kill|phase <1|2|3>` for testing.

v1 files you may modify: `ritual/AltarBlockEntity.java`, `limbo/GhostShipBuilder.java`,
`lives/BanService.java` (batch revive entry only — do not change single-unban semantics),
`registry/EclipseItems.java`, `registry/EclipseSounds.java`,
`core/state/EclipseWorldState.java`, `admin/EclipseCommands.java`,
`network/EclipsePayloads.java` (S2CShakePayload), lang, sounds.json, `README.md`.

Acceptance: build green. `runServer`: `/eclipse boss ferryman summon` on the limbo ship
(`/eclipse tp_limbo` first) spawns with themed bar; `/eclipse boss ferryman phase 2`
raises hostile deckhands + extinguishes lanterns (probe blocks/entities); phase 3 raises
water one layer (probe); `kill` triggers the mass-revive path with zero banned players
without erroring. Stop by PID.

Commits:
`eclipse v2: ferryman boss model + uv doc + shake payload`
`eclipse v2: ferryman three-phase ship fight, ghost lanterns, sinking + mass-revive finale`

---

## Worker 13 — Umbral-shard economy, reward items, improved 14-day arc config

**Model:** sol-fast

**Prompt:**

Your job: the umbral-shard reward economy and the v2 14-day arc config defaults. Read
`docs/ideas/04_content.md` §4, §5 (table only — downloads happen later) and §6 IN FULL.
Existing pieces: `umbral_shard`, `herald_core`, `ferryman_toll` items (W10–W12),
`heart_fragment` (v1), hearts cap `HeartsService.MAX_HEARTS = 7` (W2), altar
(`ritual/AltarBlockEntity`), graves (`lives/GraveBlock*`), announce payloads (W8).

Tasks:
1. **Shop** (`economy/ShardEconomy`): sneak-depositing umbral shards at the altar credits a
   per-player shard balance (new persisted player attachment `eclipse:shards`, int,
   default 0, `copyOnDeath()` — add to `registry/EclipseAttachments`). While sneaking at
   the altar, cycle the reward list on the ACTION BAR (v1 hint pattern) every 20t; punch
   (left-click) while sneaking = buy current. Team purchases draw from a pooled counter in
   `EclipseWorldState` (add `shardPool`).
2. **Reward items** (specs verbatim from §4 table; all registered in `EclipseItems`,
   16×16 programmer-art icons via `scripts/placeholder_gen/`, lang en_us + de_de):
   - `compass_of_watcher` (8 shards): compass whose `inventoryTick` writes the NEAREST
     OTHER PLAYER's `GlobalPos` into the `minecraft:lodestone_tracker` component every 40t.
     Never reveals who.
   - `grave_dowser` (4): same trick → holder's nearest own grave. Maintain
     `EclipseWorldState.gravePositions` (owner UUID → List<GlobalPos>): append where
     `lives/LifecycleEvents` places a grave, remove on grave break/scatter/loot
     (`lives/GraveBlock`/`GraveBlockEntity` removal paths).
   - `vitae_shard` (12): consumable, 32t use duration, TOTEM_USE sound, +1 heart via
     `LivesApi.add(player, 1)` capped at `HeartsService.MAX_HEARTS`.
   - Umbral tools (SHOULD, do after the three above): `umbral_pick` (+mining speed under
     open sky at night — `BlockEvent.BreakSpeed` handler), `umbral_blade` (+1 heart
     lifesteal on player kill — hook `LifecycleEvents` kill path, respect cap);
     unrepairable, high durability. 10–16 shards.
   - Team `supply_beacon` (24 pooled): drops a falling supply crate (barrel with curated
     loot table `data/eclipse/loot_table/supply_crate.json`) 50–100 blocks from the altar
     with an END_ROD Quasar column (`S2CQuasarPayload`); coordinates never announced.
3. **v2 arc config defaults** (edit ONLY the DEFAULTS written by
   `core/config/EclipseConfig` — the schema stays): rewrite `days.json` defaults to the §6
   table: day 2 unlocks `nether` + `main_inventory`; day 3 `workbenches`+`create`; day 4
   `armor`+`farmersdelight`+`simulated`; day 5 `aeronautics`(milestone L3)+`supplementaries`;
   day 7 `enchanting` gated by `herald_slain` (W11's derived key — put `enchanting` in the
   day-7 unlocks AND note in README it activates only once the Herald falls; implement by
   having `UnlockState` union day-7's `enchanting` only when `heraldDefeated` — W11 built
   the flag; extend `UnlockState` accordingly if W11's `herald_slain` union doesn't already
   cover it); day 8 `ender_chests`+`sophisticatedbackpacks`+`sable`; day 9
   `brewing`+`createaddition`; day 10 `smithing`; day 12 `end`. Remove borderSize reliance
   (W7 deprecated it — set values anyway for backward compat). `milestones.json` defaults:
   L1 16 iron, L2 16 gold, L3 8 diamonds, L4 herald_core×1 + 16 ender pearls, L5 2
   netherite ingots (item ids in the existing ItemCost schema). `modgate.json` defaults +=
   namespaces `farmersdelight`, `supplementaries`, `sophisticatedbackpacks`,
   `createaddition` (unlock keys = same names). Do NOT gate libraries
   (`sophisticatedcore`, `moonlight`).
   IMPORTANT: existing `config/eclipse/*.json` in run dirs are stale — delete
   `run/config/eclipse/` (if present) or document that defaults only apply to fresh
   configs; also update the README day table.
4. **Goal ticking**: wire real per-player goal completion into W8's
   `S2CGoalProgressPayload` where trivially detectable (e.g. "enter the nether" via
   dimension-change event, "reach altar" via proximity). Non-detectable goals stay
   manual: `/eclipse goals tick <player> <index>` admin command. Store per-player progress
   in a new attachment `eclipse:goal_progress` (int bitmask per day, reset on day change).

v1 files you may modify: `core/config/EclipseConfig.java` (defaults only),
`registry/EclipseItems.java`, `registry/EclipseAttachments.java`,
`ritual/AltarBlockEntity.java`, `lives/LifecycleEvents.java`, `lives/GraveBlock.java`,
`lives/GraveBlockEntity.java`, `progression/UnlockState.java`, `admin/EclipseCommands.java`,
`core/state/EclipseWorldState.java`, lang files, `README.md`.

Acceptance: build green. `runServer` with FRESH config: log shows 14 day plans with the new
unlocks; `/eclipse status` reflects day-1 locks; give yourself shards via a temporary admin
path? — no: add `/eclipse shards set <player> <n>` admin command (keep it, it's a dev tool);
buy a vitae shard and verify `LivesApi` interaction compiles; compass registered
(`/give @s eclipse:compass_of_watcher` succeeds). Stop by PID.

Commits:
`eclipse v2: umbral shard economy + watcher compass/grave dowser/vitae shard/supply beacon`
`eclipse v2: v2 14-day arc defaults (nether day 2, boss days 7/14, gated mods) + goal ticking`

---

## Worker 14 — Dev tools: stage step-loader, phase scheduler, goal editor GUI, inspector

**Model:** fable-xhigh

**Prompt:**

Your job: the operator tooling suite. Read `docs/ideas/05_systems.md` §3 IN FULL (command
tree) and `docs/ideas/01_world_terrain.md` §C ("Dev stage tooling", "Pristine snapshots").
Existing pieces: `WorldStageService`/`RingGrowthService` (W4), `FreezeService` (W6),
`CutscenePaths` editor commands (W6), `EclipseConfig` JSON + reload (v1).

Tasks:
1. **Stage step-loader** (`devtools/StageIO`): a "stage" here = a saved snapshot of the ring
   annulus between stage N−1 and N radii.
   `/eclipse stage save <n>` serializes all chunk SECTIONS intersecting the annulus to
   `<world>/eclipse/stages/<n>.bin` — raw section palettes + block entities via NbtIo
   (StructureTemplate chokes at millions of blocks; write your own compact format:
   per-chunk [pos, section y-range, palette, packed states, BE NBT list]).
   `/eclipse stage load <n>` applies via the same tick-budgeted writer style as
   RingGrowthService (reuse its budget/relight/resend machinery — refactor a shared
   `BudgetedBlockWriter` out of it if clean). `/eclipse stage revert` re-applies the
   last-loaded snapshot (persist "last loaded" in EclipseWorldState).
   `/eclipse stage status` prints stage, cursor, in-flight jobs.
   Workflow this enables: load N → hand-edit terrain → save N → later load N+1.
   Also **pristine world snapshots**: `/eclipse stage snapshot save|restore <name>` —
   flush (`ServerChunkCache.save(true)`), copy `region/ entities/ poi/` dirs to
   `<world>/eclipse/stage_snapshots/<name>/`; restore writes a marker file consumed at
   `ServerAboutToStartEvent` (requires restart — say so in the command feedback).
2. **Phase scheduler** (`devtools/PhaseScheduler`): `nextPhaseEpochMillis` in
   `EclipseWorldState` (add field). `/eclipse schedule next <ISO8601|+NhNNm>` (parse both
   `2026-08-01T18:00` server-local and `+2h30m`), `/eclipse schedule list | clear`.
   `ServerTickEvent.Post` check every 100t: when now ≥ target → `DayScheduler.setDay(day+1)`
   + clear. Countdown: ONE lazy global `ServerBossEvent` ("Next phase: 2h 14m", purple,
   progress = remaining/total, themed `day` via W8's style payload), hidden when idle.
   Persists across restarts (absolute epoch). If `dayAutoAdvance=true` simultaneously, log
   a warning that the scheduler supersedes it and skip auto-advance while a schedule is set
   (edit the guard in `progression/DayScheduler`).
3. **Freeze/invuln commands**: `/eclipse freeze <players> on|off`,
   `/eclipse invuln <players> on|off` — thin wrappers over `FreezeService` (add an
   invuln-only mode flag to FreezeService: cancel damage/knockback without movement lock).
4. **Timeline inspector**: `/eclipse timeline` prints (source-only, v1 style): day, altar
   level, per-dim stage + ring radius + growth cursor, schedule target + remaining, active
   cutscenes + per-player freeze/ACK state, night event, herald/ferryman flags, soft border
   radius, watchdog events since boot (keep a small ring buffer in FreezeService).
5. **Goal/unlock editor GUI** (`devtools/client/GoalEditorScreen`): opened by
   `/eclipse goals edit` (server sends an open-screen payload — add
   `S2COpenGoalEditorPayload` carrying current days.json content). Client screen lists days
   1–14 → per day: editable goal lines (EditBox list), unlock keys (comma field), border
   (legacy field). Save → `C2SConfigEditPayload(String fileName, String json)` —
   server-side: REQUIRE `hasPermission(3)`, validate JSON parses into the DayPlan schema,
   write `config/eclipse/days.json`, `EclipseConfig.reload()`, broadcast day state +
   announce. Reject oversize payloads (>64 KB).

v1 files you may modify: `core/state/EclipseWorldState.java`,
`progression/DayScheduler.java`, `admin/EclipseCommands.java`,
`network/EclipsePayloads.java`, `worldgen/stage/RingGrowthService.java` (refactor only),
`cutscene/FreezeService.java` (invuln-only mode + ring buffer), `README.md`, lang files.

Acceptance: build green. `runServer`: `/eclipse stage save 1` writes a .bin (check file);
edit a block in the annulus, `/eclipse stage load 1` restores it (probe with
`/execute if block`); `/eclipse schedule next +0h1m` shows the countdown bar and advances
the day after a minute (wait for it); `/eclipse freeze @s on` rubber-bands (verify via a
second command probe or log); `/eclipse timeline` dumps every field. Stop by PID.

Commits:
`eclipse v2: stage step-loader (annulus save/load/revert) + pristine snapshots`
`eclipse v2: phase scheduler + freeze/invuln commands + timeline inspector + goal editor gui`

---

## Worker 15 — Main menu v2 + in-game settings screen

**Model:** sol-fast

**Prompt:**

Your job: upgrade the custom title screen to an animated v2 and add a settings screen.
Read `docs/ideas/03_ui_ux.md` §A ("Title animation", "Hi-res art", "Toggle persistence")
and §F.10. Existing pieces: `client/menu/EclipseTitleScreen` (custom title screen with
static background + custom buttons), `TitleScreenSwap` (swaps vanilla title → ours; keep
its guard pattern), `OptionsThemer`, `EclipseClientConfig` (W1: `customMenu` toggle exists).

Tasks:
1. **Animated background**: the existing 6 panorama faces
   (`assets/eclipse/textures/gui/title/panorama_*.png`) become a slowly drifting
   `PanoramaRenderer`/`CubeMap` (vanilla-style spin, slower). On top: 3 parallax layers
   (`assets/eclipse/textures/gui/title/parallax_{far,mid,near}.png`, 1024×512, transparent —
   programmer-art placeholders via `scripts/placeholder_gen/`) offset opposite the mouse at
   2/4/8 px; screen-space purple wisp particles (simple client-side particle list rendered
   in `render()` — spawn 1–2/s, drift up, fade; cap 40; honor `reducedFx`); subtle vignette.
   Logo gets a periodic eclipse-flare sweep (a bright gradient quad swept across the logo
   texture every ~12 s).
2. **Fully custom widgets**: extend the existing `EclipseMenuButton` styling to ALL widgets
   on the title screen (buttons, small icon buttons); hover = glow + `eclipse:ui.hover`
   sound (registered by W9 — reuse; if W9's sound ids are absent, register them here
   following the same pattern). Keep accessibility/narration intact
   (use vanilla Button subclasses, don't hand-roll focus handling).
3. **Config toggle**: `customMenu=false` in `eclipse-client.toml` must restore the VANILLA
   title screen entirely — `TitleScreenSwap` already guards the swap; wire it to
   `EclipseClientConfig.customMenu()` and verify the toggle is read live (screen-open time
   is fine, no restart).
4. **Settings screen** (`client/menu/EclipseSettingsScreen`): toggles for every
   `EclipseClientConfig` value (customMenu, showBossbarSkin, showSidebar, uiSounds,
   customCursor, veilPostFx, reducedFx) using vanilla `AbstractSliderButton`/`CycleButton`
   widgets, styled like the title screen. Writes through ModConfigSpec (`.set()` + save).
   Reachable from: (a) the Mods list via `IConfigScreenFactory` extension point
   (register in the mod constructor or client setup:
   `modContainer.registerExtensionPoint(IConfigScreenFactory.class, ...)`), (b) a small
   gear button on the custom title screen, (c) in-game: a "Settings" button in the
   handbook's Status tab footer (edit `client/handbook/` minimally — one button).
5. Lang keys en_us + de_de for every new label.

v1 files you may modify: `client/menu/EclipseTitleScreen.java`,
`client/menu/TitleScreenSwap.java`, `client/menu/EclipseMenuButton.java`,
`client/menu/OptionsThemer.java`, `EclipseMod.java` (extension point),
`client/handbook/tabs/` (one button), lang files, `README.md`.
Do NOT modify `EclipseClientConfig` keys (only read/write values).

Acceptance: build green. If the environment allows, `./gradlew runClient`: title screen
animates (attach a screenshot), gear opens settings, toggling `customMenu` off + reopening
title shows vanilla; kill client by PID. Otherwise code-verify + build and say so.

Commits:
`eclipse v2: animated title screen (drifting panorama, parallax, particles, flare)`
`eclipse v2: settings screen + config screen factory + handbook settings button`

---

## Worker 16 — FINAL integration: mod downloads, matrix smoke, fixes, docs

**Model:** fable-xhigh

**Prompt:**

Your job: final integration. Download the real mod pack, run the full matrix, fix what
breaks, and update the docs. Read `docs/ideas/04_content.md` §5, `docs/ideas/05_systems.md`
§4, and `docs/PLAN_V2.md` §7 (final checklist). You may fix bugs in ANY v2 file; keep fixes
minimal and note each in your final message.

Tasks:
1. **Download mods** from Modrinth (use the Modrinth API v2:
   `https://api.modrinth.com/v2/project/<slug>/version?game_versions=["1.21.1"]&loaders=["neoforge"]`,
   pick the exact versions below, download the `.jar` files) into `run/mods/`
   (client+server dev runs share it). Do NOT commit jars to git — add `run/mods/*.jar` to
   `.gitignore` if not already ignored (check `run/` ignore status first; v1 already had
   sodium/iris jars there).
   | Slug | Version (verified 2026-07) | Namespace |
   |---|---|---|
   | `veil` | 4.3.0 (veil-neoforge-1.21.1-4.3.0.jar) | veil (required dep!) |
   | `farmers-delight` | 1.21.1-1.3.2 | farmersdelight |
   | `supplementaries` | 1.21.1-3.8.3 (+ its Moonlight Lib dependency) | supplementaries |
   | `sophisticated-backpacks` | 1.21.1-3.25.71 (+ `sophisticated-core`) | sophisticatedbackpacks |
   | `createaddition` (Create: Crafts & Additions) | neoforge-1.21.1-1.6.0 | createaddition |
   | `create` | 6.0.10 (or the version Aeronautics pins — VERIFY first) | create |
   | `sable` | mc1.21.1-2.0.3-neoforge | sable |
   | Create: Aeronautics (project `oWaK0Q19`) | 1.3.x line, 1.21.1 NeoForge (+ `simulated` if separate) | aeronautics, simulated |
   | Simple Voice Chat | voicechat-neoforge-1.21.1-2.6.16 | voicechat |
   | `sodium` | 0.6.13 (already in run/mods) | — |
   | `iris` | 1.8.12 (already in run/mods) | — |
   Check Aeronautics' required Create version BEFORE choosing the Create jar (its
   `neoforge.mods.toml` inside the jar states the range).
2. **mods.toml deps**: add optional `AFTER` dependencies for `farmersdelight`,
   `supplementaries`, `sophisticatedbackpacks`, `createaddition` in
   `src/main/templates/META-INF/neoforge.mods.toml` (match the existing optional-dep
   blocks). Eclipse must still load with NONE of the optional mods present.
3. **Supplementaries anonymity gap**: Supplementaries adds sign-like text blocks (notice
   boards etc.). Extend `anonymity/TextInputBlocker` to cancel place/interact for any
   block whose registry id namespace is `supplementaries` and path contains sign-like
   words, or (better) cancel its text-setting GUIs generically: block right-click on
   `supplementaries` blocks that open a text screen. Namespace-string matching only — NO
   compile-time dependency.
4. **Smoke matrix** (fix breakages between runs; document each):
   a. `./gradlew build` green.
   b. `runServer` with FULL mod pack, fresh world: boots to Done; `/eclipse status`;
      `/eclipse day set 4` (farmersdelight unlock announce); `/eclipse stage set overworld 2
      animate` completes; modgate sweep doesn't strip library mods; stop by PID.
   c. `runServer` with ONLY eclipse + veil (move other jars out temporarily): boots, day
      set works — proves optional-mod independence. Restore jars.
   d. `runClient` full pack (Sodium+Iris, no shaderpack): join a world; run the FINAL
      CLIENT VISUAL EVAL CHECKLIST from `docs/PLAN_V2.md` §7 — screenshot each numbered
      item you can reach (title v2, handbook tabs, hearts burst via `/eclipse lives`,
      border FX at `/eclipse border ring set 60`, limbo grade via `/eclipse tp_limbo`,
      one boss summon). Attach screenshots to your final message.
   e. `runClient` with a shaderpack installed in `run/shaderpacks/` if trivially
      downloadable (e.g. Complementary from Modrinth) + enabled: verify Veil pipelines
      self-disable (log line) and the game renders. If no pack is obtainable, verify the
      gate by code + log injection and say so.
   f. Aeronautics: on the full server, verify it loads and its namespaces are gated day-1
      (`/eclipse status` shows locks). Airship-vs-border physical test is best-effort in
      dev; document the sub-level caveat in README (player teleport fallback covers it).
5. **Docs**: update `README.md` — server-pack table (new mods + versions + gated
   namespaces), v2 feature summary section, new command tree, new config files, day table
   (v2 arc), known-limitations refresh (Veil+shaderpack degradation, Sable border caveat,
   fresh-world requirement). Update `AGENTS.md` Cursor Cloud section: Veil is required at
   runtime; fresh world after min_y change; how to run the visual checklist; that
   placeholder art lives at ASSET_MANIFEST paths.
6. Bump `mod_version` in `gradle.properties` to `2.0.0`.

v1 files you may modify: any (bug fixes), plus `README.md`, `AGENTS.md`,
`src/main/templates/META-INF/neoforge.mods.toml`, `gradle.properties`, `.gitignore`.

Acceptance: the full matrix above, all green, with evidence (logs + screenshots) in your
final message.

Commits:
`eclipse v2: optional-mod deps + supplementaries text-input blocking`
`eclipse v2: integration fixes from full-pack smoke matrix`
`eclipse v2: v2 docs, day table, known limitations + version 2.0.0`

---

## Sequencing summary

| # | Worker | Depends on | Model |
|---|---|---|---|
| 1 | Veil foundation (dep, gates, post shaders, Quasar) | — | fable-xhigh |
| 2 | Hearts (max health + burst) | 1 (heart_burst emitter, config) | sol-fast |
| 3 | Disc world core (generator, biomes, height) | — | fable-xhigh |
| 4 | Stages (ring growth, fusion, nether, triggers) | 3, 1 (Quasar) | fable-xhigh |
| 5 | Structures (stamper, sanctum, stronghold) | 4 | fable-xhigh |
| 6 | Cutscene engine + freeze + intro | 1 (easing), 4 (unlock hooks) | fable-xhigh |
| 7 | Soft border + glitch FX | 4 (radii), 6 (freeze), 1 (post) | fable-xhigh |
| 8 | HUD: bossbars, sidebar, announcements, timeline | 1 (config), 4/7 (events) | fable-xhigh |
| 9 | Handbook 2.0 | 8 (timeline payload), 1 (config) | fable-xhigh |
| 10 | Mobs + spawner + night events | 8 (announce), 1 (Quasar) | fable-xhigh |
| 11 | Herald boss | 10 (stalkers), 5 (arena), 8 (bar theme) | fable-xhigh |
| 12 | Ferryman boss + finale | 10 (deckhands), 6 (cutscene), 11 (pattern) | fable-xhigh |
| 13 | Economy + v2 arc config | 10–12 (items), 2 (heart cap) | sol-fast |
| 14 | Dev tools suite | 4, 6 (services to drive) | fable-xhigh |
| 15 | Menu v2 + settings | 1 (config), 9 (sounds) | sol-fast |
| 16 | Integration + downloads + matrix + docs | all | fable-xhigh |
