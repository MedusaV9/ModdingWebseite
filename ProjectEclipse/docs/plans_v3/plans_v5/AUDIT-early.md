# AUDIT-early — verification of every v2.1 + v3 (early-round) task against CURRENT code

**Auditor**: AUDIT-1 (read-only; grep/read verification of mechanism existence AND wiring — no gradle, no git).
**Scope**: the 74 early-round items listed by the user. Status per item: **DONE** (mechanism exists and is wired, evidence file), **PARTIAL** (gap named), **MISSING**.

**Result: 73 DONE · 1 PARTIAL · 0 MISSING.**

Wiring standard applied: a "DONE" required the class/data to be reachable at runtime — `@EventBusSubscriber`/`@SubscribeEvent` on the class, a registration call from `EclipseMod`/registries, a registered payload/command, or a datapack file in a live-loaded path. File paths below are relative to `src/main/java/dev/projecteclipse/eclipse/` (java) or `src/main/resources/` (assets/data) unless noted.

| # | Item | Status | Evidence / gap |
|---|------|--------|----------------|
| 1 | EN/DE localization everywhere + `/lang` + `/sprache` | **DONE** | `assets/eclipse/lang/en_us.json` + `de_de.json` (2 132 lines each, parity enforced by `tools/langmerge/merge_langdrops.py`); client override commands in `client/lang/LangClientCommands.java` (registers both `lang` and `sprache` literals); runtime locale merge in `client/lang/EclipseLang.java`; server-side per-player translation via `lang/ServerLang` (used across commands). |
| 2 | VFX settings accessible without CustomTitle | **DONE** | `client/menu/EclipseSettingsScreen.java` is standalone; mounted from the pause menu (`client/menu/PauseMenuHook.java`), the Mods-list config factory (`client/menu/ClientMenuExtensions.java` registers `IConfigScreenFactory`), and the in-game handbook (`client/handbook/tabs/SettingsTab.java`) — no title-screen dependency. All VFX toggles live in `client/menu/SettingsPanel.java`. |
| 3 | Custom dimension-change loading screens | **DONE** | `client/loading/LoadingScreenSwap.java` swaps `ReceivingLevelScreen` (every dimension change incl. NeoForge transition-screen factory) and `LevelLoadingScreen` for `EclipseLoadingScreen` via `ScreenEvent.Opening`; killswitch `customLoadingScreens`; glitch portal transitions in `client/loading/PortalTransitionController.java`. |
| 4 | Chunk-pop-in fix + terrain quality + vanilla-like caves/biomes | **DONE** | Pop-in fix: animated wavefront sweep with client column-rise + reveal delay (`worldgen/stage/GrowthPacing.java` — `revealDelayTicks`/`columnRiseTicks`/`S2CGrowthWavePayload`; `worldgen/stage/RingGrowthService.java`). Terrain/caves: real vanilla pipeline on the disc — carvers, per-biome placed features, cave biomes (`worldgen/vanilla/DiscGenPipeline.java`, `DiscCarverEngine.java`, `worldgen/CaveBiomeMap.java`, `CaveEntrances.java`, `CaveDressings.java`, `vanilla/BiomeFeatureFilter.java`). |
| 5 | Mob spawning on disc | **DONE** | `worldgen/DiscChunkGenerator.java` uses real per-biome `BiomeGenerationSettings` + `spawnOriginalMobs` (animal seeding); natural monster spawning works off the real biome mob settings; event-specific rules in `entity/spawn/EventSpawnRules.java` and `entity/EclipseSpawner.java`. Spawn pinning: `worldgen/DiscSpawnPlacement.java`. |
| 6 | Ravines | **DONE** | Vanilla cave/canyon carvers run on the disc base — `worldgen/vanilla/DiscCarverEngine.java` ("caves, extra underground caves, canyons"); `DiscChunkGenerator.applyCarvers`. |
| 7 | Water/lava/stone variety | **DONE** | Strata (deepslate/tuff bands, per-sector palettes), lava-fall curtains, lava-spring pits: `worldgen/DiscTerrainFunction.java`; cave spring lakelets/dripstone: `worldgen/CaveDressings.java`; vanilla springs/geodes/stone-variety blobs deliberately KEPT by the ore-only deny list (`worldgen/vanilla/BiomeFeatureFilter.java` doc: only mineral ores denied). |
| 8 | Dungeons underground | **DONE** | `worldgen/structure/UndergroundSites.java` — deterministic per-annulus mineshafts, vanilla-style monster rooms at shaft bottoms, plus custom dungeons (Collapsed Vault, Flooded Crypt, Glitch Reliquary, Umbral Warrens) under `worldgen/structure/dungeon/`. |
| 9 | Supply drops: no lag, beam disappears after loot, Veil not particles | **DONE** | `economy/SupplyBeacon.java` — lifecycle explicitly "fixes 'the beam never disappears'": marker binds to the landed barrel, loot/break/expiry fades the beam ≤ 2 s (`REMOVE_FADE_TICKS`); beam rendered client-side from one marker payload (no per-tick particle spam) via `veilfx/SupplyBeamClient.java` + `SupplyBeamRenderer.java` + quasar emitter `assets/eclipse/quasar/emitters/altar_beam.json`. |
| 10 | Inventory slot locking with hidden slots | **DONE** | Server-authoritative sweeps `progression/PhaseInventoryLock.java` (+ sync `progression/InvLockSync.java`); client "slot does not exist" illusion incl. click/hotkey swallowing + unseal animation in `client/invlock/InvLockOverlay.java` + `InvLockClientState.java`. |
| 11 | Border strong + only visible nearby | **DONE** | Server-authoritative circular soft border with pushback/teleport-clamp per disc dimension: `border/SoftBorder.java`; client FX invisible until within synced `fxRange` (default 8 blocks) then fades in: `border/client/BorderFxRenderer.java` (`fxIntensity` 0 until `ringDist < fxRange`). |
| 12 | Structures not on trees | **DONE** | `worldgen/structure/StructureStamper.java` — every vanilla placement goes through `SitePrep.Mode.PLATEAU` terraform; fallbacks build "on a SitePrep'd pad instead — never on raw (tree-covered) ground" (`worldgen/structure/SitePrep.java`). |
| 13 | Nether 1:1 disc below bedrock + breach animation + rim smoke + glitch transfer both ways | **DONE** | 1:1 nether disc (`DiscProfile.NETHER` radii 150/280/440 in `core/config/EclipseConfig.defaultStages`, "no 8:1 mapping divide" — `worldgen/nether/BreachTransferService.java`); breach carve/animation phases + opening smoke (`worldgen/nether/BreachBuilder.java` `emitOpeningSmoke`, `S2CBreachPayload`); persistent rim/mouth smoke curtain + glitch-drift descent AND soul-updraft return, both hidden inside a client glitch pulse (`BreachTransferService.java`). |
| 14 | End spawns with loud crash mid-map + dragon fight | **DONE** | `worldgen/end/EndDiscService.java` — `announceCrash` at disc center: `S2CEndCrashPayload` + global shake + `ENDER_DRAGON_GROWL` at volume 4.0; custom fight `worldgen/end/EclipseDragonFight.java` (crystals, boss bar, rewards, victory listener) + `EndShatterSequence.java`. |
| 15 | Nether/end discs carry their biome structures | **DONE** | Nether: `eclipse:fortress_core` stage-1 structure (`FortressCoreBuilder.java`), inverted-bastion hanging court + top-side vanilla bastion-remnant (`worldgen/nether/HangingCourtBuilder.java`); End: end-city towers/ship with real loot + shulkers (`worldgen/end/EndCityKit.java`) + purpur spires/crystals (`EndSpires.java`). |
| 16 | Heartbeat-disable setting | **DONE** | `core/config/EclipseClientConfig.HEARTBEAT_SOUND` (`heartbeatSound`), toggle row in `client/menu/SettingsPanel.java` (line ~147), consumed by `hearts/client/HeartBurstOverlay.java`. |
| 17 | Event-start wave effect replaced | **DONE** | `client/WaveOverlay.java` v2 — "The v1 flat tiled wave texture is gone"; submerge now renders refractive shockwave rings via the `eclipse:shockwave` Veil post pipeline (`assets/eclipse/pinwheel/shaders/program/shockwave.fsh`), with Iris/reducedFx fallback. |
| 18 | Stages auto-save on load | **DONE** | `worldgen/stage/WorldStageService.java` — stages persisted in `EclipseWorldState`, republished into the chunkgen seam BEFORE spawn chunks on world load, interrupted sweeps resume from persisted cursor on `ServerStartedEvent`. |
| 19 | Players spawn in Limbo pre-event | **DONE** | `limbo/LimboGate.java` (`@EventBusSubscriber`; login/respawn outside limbo → brought to ghost ship while the event hasn't started) + `limbo/PreEventSafety.java`. |
| 20 | Limbo Veil effects + crazy water + eclipse overhead with aura + no clouds | **DONE** | Veil ambience emitters + god rays + water-hugging fog (`veilfx/LimboAmbience.java`); water warp/caustics post pipeline (`assets/eclipse/pinwheel/shaders/program/limbo.fsh`, world-anchored caustic UVs) + graveyard seascape (`limbo/LimboSeascape.java`); eclipse disc dead-center overhead with counter-rotating ray aura + mirror-locked reflection + `renderClouds` no-op (`client/sky/LimboSpecialEffects.java`). |
| 21 | Limbo ship boss fight; passive mobs standing double; unhittable fishers fixed incl. after death | **DONE** | Ferryman boss fight (`entity/boss/FerrymanEntity.java`, `ferryman/ArenaFight.java`); duplicate-rower fix = bug 4a bench-identity reconcile (async entity-section resolve window), unhittable-sagging fix = bug 4b `calmCrew` reseat + per-entity orphan self-heal after boss death/`/kill`/crash — all documented and implemented in `entity/DeckhandEntity.java`. |
| 22 | Smaller early map | **DONE** | D8-rebalanced stage radii: stage 1 = r 150 (was larger pre-overhaul; legacy radii kept only for old saves) — `core/config/EclipseConfig.defaultStages`, `worldgen/FrozenParams.java` (`DEFAULT_*_RADII` vs legacy `SCAR_RADII`). |
| 23 | Same-map-different-saves bug | **DONE** | `worldgen/FrozenParams.java` — per-save freeze of ALL terrain-shaping params to `world/<save>/eclipse/worldgen.json`, written on first boot, read before dimensions deserialize every start. |
| 24 | Altar harder + schedule simplification | **DONE** | Multi-cost altar milestones with per-item counters (`ritual/AltarBlockEntity.java`, `altar_level_<n>:<item_id>` keys; costs from `EclipseConfig` milestones); schedule unified behind one server-authoritative day clock + real-time engine with v5 interval cadence (`progression/DayScheduler.java`, `progression/realtime/RealtimeDayService.java`). |
| 25 | Sky truly darkens | **DONE** | `veilfx/VeilPostController.java` — "consolidated night/eclipse grade (R3/R16) — the 'sky never darkens' fix" (world_grade pipeline); eclipse-driven star/sky handling in `client/sky/OverworldPurpleEffects.java`. |
| 26 | Glowing point aligned with purple sun | **DONE** | `veilfx/SunTracker.java` — single source of truth updated from the exact AFTER_SKY matrices; explicitly "the sun-halo misalignment fix" (old `sun_halo.fsh` used bobbing-stripped Veil camera). Sky quad + halo both read it (`client/sky/OverworldPurpleEffects.java`). |
| 27 | Handbook rework: top-right inventory slot, right-click open, sounds | **DONE** | Artifact pinned to main-storage index 17 = top-right inventory slot (`artifact/ArtifactSlotLock.java`), slot ring/badge (`client/handbook/InventorySlotDecor.java`); `use()` right-click opens (`artifact/ArmArtifactItem.java` → `client/ArtifactScreenOpener`); UI sound suite (`client/handbook/UiSounds.java`, page-turn jitter etc.). |
| 28 | Cutscene text visibility + out-of-render-distance handling | **DONE** | Captions immune to cutscene HUD suppression by construction (letterbox whitelist layer, `cutscene/client/CaptionRenderer.java` — replaces the old silently-cancelled subtitles); far players gathered via `TeleportPolicy.GLOBAL_TELEPORT` + path chunk-preload tickets + view-distance bump (`cutscene/CutsceneService.java`, `cutscene/ViewDistanceService.java`). |
| 29 | Ship oars movement | **DONE** | GeckoLib rower+oar single skeleton, 60-tick `row` loop that can never desync (`entity/DeckhandEntity.java`); legacy display cleanup + cutscene tilt flag (`limbo/OarAnimator.java`). |
| 30 | Altar+spawn surroundings improved + altar level system | **DONE** | Sanctum crater/floating sanctum/orbitals/sundial plaza/watcher statues (`worldgen/structure/SanctumCrater.java`, `FloatingSanctumBuilder.java`, `SanctumOrbitals.java`, `SundialPlaza.java`, `WatcherStatues.java`); altar level milestones raise + sync level (`ritual/AltarBlockEntity.java`). |
| 31 | Goal editor with real triggers | **DONE** | `/eclipse goals edit` opens client editor GUI via `S2COpenGoalEditorPayload`, save re-checks permission server-side (`admin/EclipseCommands.java`, `devtools/ConfigEditor.java`); goals migrated to `GoalSpec`s with real triggers (`progression/goals/GoalConfig.java`, `TriggerType.java`, `QuestDetectors.java`, `QuestEngine.java`). |
| 32 | 3–5 side quests/day | **DONE** | Authored per-day sides in the default `goals.json` (dozens of `side(...)` entries across days; `sidesForDay`) — `progression/goals/GoalConfig.java`. |
| 33 | Recipe limiting per day + base-building stats rewards | **DONE** | Day-tier craft locks + smithing strip + EMI sync (`progression/RecipeGate.java`, `RecipeGateConfig.java`); building stats tracked (`analytics/PlacedBlockTracker.java`, `place_total` key) and rewarded via the `master_builder` award category (`awards/AwardConfig.java`). |
| 34 | Altar daily offerings with secret values + daily winner | **DONE** | `offering/OfferingService.java` — exact-value accounting where the value stays secret (`accept` returns the secret exact value), rollover resolution picks winner(s) + `winnerReward` split; deposit values in `analytics/DepositValues.java`, rules/config in `offering/OfferingRules.java`/`OfferingConfig.java`. |
| 35 | Bossbar improved | **DONE** | `client/hud/BossbarSkin.java` v3 — payload-tagged bars, entrance/exit state machine, damage-chip ghost fill, themes, `bossbarStyle` variants; style payload `S2CBossbarStylePayload`. |
| 36 | Dev commands: timed buffs anti-abuse; timer pause/add/reduce animated; timeline color; render distance server-side + player toggle | **PARTIAL** | Buffs: `/dev buff give/clear/list` forced server-global (no per-player favoritism), perm-2, operator audit broadcast (`devtools/dev/DevBuffCommands.java`, `buffs/TimedBuffService.java`). Timer: `/dev timer pause/resume/add/sub/set/status`, every mutation broadcasts `S2CDayClockPayload` driving the client spool animation (`devtools/dev/DevTimerCommands.java`). Render distance: server-side per-player persistent pins + cinematic precedence, client opt-out toggle authoritative (`devtools/dev/DevViewDistance.java`, `SettingsPanel` `server_render_distance`). **Gap**: no dedicated timeline/timer COLOR control (command or setting) exists anywhere (`rg color` across `devtools/dev/`, `timeline/`, payloads, `DEV_COMMANDS.md` — nothing); the only color behavior is the automatic urgency ramp TEXT→ACCENT→ACCENT_DEEP in `client/hud/DayTimerLayer.java`. If the auto-ramp was the accepted resolution, this item is DONE; a manual color control was never built. |
| 37 | Daily top-3 rewards with screen animation + analytics | **DONE** | Default `categoriesPerDay = 3` weighted draw with themes (`awards/AwardConfig.java` `new Data(3, …)`, `awards/AwardMath.java`), resolved at day rollover (`awards/AwardService.java`); head-roulette reveal overlay with tick/win sounds, ESC-to-summary (`client/awards/AwardsOverlay.java`, `RouletteStrip.java`); stats from the analytics engine (`analytics/AnalyticsService.java`, `AnalyticsSampler.java`). |
| 38 | Custom mobs with custom attacks + GeckoLib | **DONE** | GeckoLib 4.9.2 required dep (`gradle.properties`, `build.gradle`); shared geo base (`entity/geo/`), bosses/dungeon/fog/glitch/pale/wizard mobs with custom goals (e.g. `entity/fog/ChargedLungeGoal.java`, `GroundSlamGoal.java`, `FogBlindBurstGoal.java`; `entity/boss/FerrymanEntity.java`, `HeraldEntity.java`); geo/animation assets under `assets/eclipse/geo/` + `animations/`; bestiary docs `docs/uv/*.md`. |
| 39 | Mods bundled/tested | **DONE** | `docs/BUNDLING.md`, mrpack builder `tools/modpack/build_mrpack.py` (+ `dist/`), dev-pack fetcher `tools/modpack/fetch_dev_mods.py`, server-authoritative namespace gating `progression/ModGate.java` + anticheat allowlist self-migration (root `AGENTS.md`, README "Server pack" matrix). |
| 40 | Dev docs in repo + /dev handbook OP-gated | **DONE** | `docs/DEV_COMMANDS.md` regenerated from the live registry by `devtools/dev/DevDocsExporter.java`; `/dev` root perm ≥ 2 (or explicit dev-bypass identity, never op-granting) in `devtools/dev/DevRoot.java`; in-game dev handbook via `DevHandbookBridge.java` + `devtools/dev/handbook/`. |
| 41 | Revoke/complete quest dev commands | **DONE** | `/dev quest complete <player> <questId>` and `/dev quest revoke <player> <questId>` (`devtools/dev/DevQuestCommands.java`, `goalMutation("complete"/"revoke")`). |
| 42 | Dev blocks | **DONE** | Shipped as the "Dev blocks & items" surface: Display Wand (`devtools/display/DevToolItems.java`, absent from creative tabs, `/dev display give`) — documented in `docs/DEV_COMMANDS.md` §"Dev blocks & items" as the supported editor. |
| 43 | Custom death screen: ship respawn, hotbar heart burst, ghost healthbar, door mechanics | **DONE** | `client/death/EclipseDeathScreen.java` + `DeathScreenSwap.java` (anonymized, heart-shatter, hold-gated respawn); ship-wake respawn state machine (`client/death/DeathFlowController.java`, `lives/DeathFlowHooks.java`); hotbar heart burst (`hearts/client/HeartBurstOverlay.java`, `burst_sheet`); ghost heart row (`client/death/GhostHeartsLayer.java`); respawn door blocks/registry/cues (`limbo/door/RespawnDoorBlock.java`, `DoorRegistry.java`, `S2CDoorCuePayload.java`). |
| 44 | Revive recipe (2 hearts for 4 fragments) in handbook | **DONE** | Heart Extractor: channel 3 s, `HEART_COST = 2` hearts → `FRAGMENT_REWARD = 4` fragments, 1-heart safety floor (`ritual/HeartExtractorItem.java`); Revive Sigil recipe consumes the 4 fragments (`data/eclipse/recipe/revive_sigil.json`); handbook Revival page renders the chain data-driven from the synced recipe (`client/handbook/tabs/RevivalTab.java`); ritual `ritual/ReviveRitual.java`. |
| 45 | Map expansion: camera flythrough + chat unlocks + head roulette + rewards | **DONE** | `sequence/ExpansionSequence.java` — `unlock_ring` orbit/crane cutscene per watcher, eclipse grade ramp, slam beats; chat unlock announcements via `timeline/AnnouncementService.java` stage listener; ending fires the daily-award head-roulette hook `AwardService.sendRevealNow` (line ~604) → `client/awards/AwardsOverlay.java`. |
| 46 | Custom dungeons (trial chambers etc.) with custom mobs | **DONE** | `minecraft:trial_chambers` + `minecraft:ancient_city` cavity placers (`worldgen/structure/StructureStamper.java`); four custom dungeon builders (`worldgen/structure/dungeon/`); config-driven spawners leading with `eclipse:*` mobs (eclipse_cultist, fog_revenant, glitched_husk/tick) in `worldgen/structure/dungeon/DungeonSpawners.java`; loot `data/eclipse/loot_table/dungeon/*.json`. |
| 47 | Nether border 8x smaller | **DONE** (superseded) | The 8×-smaller spec was deliberately replaced by the later same-list redesign: the nether is now a 1:1 disc with its own stage radii and its own soft-border ring (`worldgen/nether/BreachTransferService.java` "no 8:1 mapping divide, the nether disc is now the same size"; `border/SoftBorder.java` nether ring; hard clamp `clampNether` + margin). No 8× mechanism exists — by design of the newer item 13. |
| 48 | Admin block for animated block displays | **DONE** (as wand + commands) | Persistent block-display editor: place/select/edit/delete via Display Wand item + `/dev display` commands, SavedData-authoritative, spin/bob animation params (`devtools/display/DisplayPlacerService.java`, `DisplayAnimator.java`, `DevToolItems.java`, `devtools/dev/DevDisplayCommands.java`). Implemented as an admin ITEM rather than a literal block — documented as the supported editor (`docs/DEV_COMMANDS.md`). |
| 49 | Main menu: "Reise beginnen" date-gate, server connect, no singleplayer, OP-visible settings | **DONE** | `client/menu/EclipseTitleScreen.java` — "Reise beginnen" appears with parseable `activationIso` (`eclipse-journey.toml`); pre-date clicks play the fake-error theater + DIM countdown; post-date direct-connect via `client/menu/JourneyController.java`; `modpackMode` removes Singleplayer/Multiplayer, restored by `devUnlock`/cached op flag; Eclipse settings intentionally in-game only (pause menu/handbook). Swap in `TitleScreenSwap.java`. |
| 50 | Mod identity: Eclipse Event, Sonic0810, description, logo | **DONE** | `gradle.properties` `mod_name=Eclipse Event`, `mod_id=eclipse`; `src/main/templates/META-INF/neoforge.mods.toml` `authors/credits="Sonic0810"`, German description, `logoFile="logo.png"`; `src/main/resources/logo.png` present. |
| 51 | Stronghold at map edge → REMOVED in v5 (removal held?) | **DONE** | Removal held: stage-5 default structure list is EMPTY with the B15 comment (`core/config/EclipseConfig.defaultStages`); `VanillaLandmarks.LOCATE_SITES` stronghold row removed (comment at line ~34); `client/handbook/tabs/MapTab.java` excludes it from chartable landmarks; the `eclipse:stronghold_emergence` placer stays registered ONLY so persisted pending rows of pre-B15 saves resume (`StructureStamper.java` line ~200). Known accepted caveat (per plan + EVAL-SAT-S): legacy saves whose frozen `stages.json` still lists it keep their stronghold. |
| 52 | Ancient city in big mountain | **DONE** | Landmark `eclipse:ancient_city` at (54, −129) = the mountain center, "in the mountain, y≈−40" (`worldgen/DiscMapDefaults.java`); cavity placement `placeCavity` (`StructureStamper.java`); deep-dark band under the mountain flank (`worldgen/CaveBiomeMap.java`). |
| 53 | All biomes incl pale oak | **DONE** | 8 surface sectors + radial rings ending in `eclipse:pale_garden` (`worldgen/DiscMapDefaults.java`); custom biome json `data/eclipse/worldgen/biome/pale_garden.json`; pale oak block set `registry/PaleGardenBlocks.java` + `data/eclipse/loot_table/blocks/pale_*.json`; cave biomes via `CaveBiomeMap`. |
| 54 | Mansion + pillager outpost | **DONE** | Landmarks (`DiscMapDefaults.java` lines ~148/151) + async vanilla placers with retry (no procedural fallback → retried with adjusted anchor) (`StructureStamper.java` lines ~188–193); stage entries 2 and 4 (`EclipseConfig.defaultStages`). |
| 55 | Glitched mobs in expansions for heart crafting | **DONE** | Interval spawner on freshly-expanded terrain via `NewRingRegistry` freshness (`glitch/GlitchSpawnService.java`); `#eclipse:glitched` tag drops glitch shards (`glitch/GlitchDrops.java`); shards craft the permanent-heart Vitae Shard (`data/eclipse/recipe/vitae_shard_glitch.json`, `economy/VitaeShardItem.java`), recipe day-gated in `recipegate.json`. |
| 56 | Expansion: terrain first, then structures with glitch/rift animation | **DONE** | Two-phase contract: terrain sweep completes → sites enqueued in `worldgen/structure/StructurePendingRegistry.java` → placers build with rift reveals (`worldgen/stage/StructureFlightFx.java`, `veilfx/rift/`, `assets/eclipse/pinwheel/shaders/program/rift_glitch.fsh`); cinematic slams/shockwaves in `sequence/ExpansionSequence.java`. |
| 57 | Void ship animations + altar + spawn island | **DONE** | Ghost ship build + keel-over cutscene + lantern/crew animation (`limbo/GhostShipBuilder.java`, `StartEventCutscene.java`, `ShipLanterns.java`, `OarAnimator.java`, GeckoLib deckhands); altar (`ritual/AltarBlock*.java`) on the floating sanctum spawn island (`worldgen/structure/FloatingSanctumBuilder.java`, `SanctumOrbitals.java`). |
| 58 | 2–3 fog storm sites with special loot (Replant/Mending) + strong fog mobs | **DONE** | Two authored sites `eclipse:fog_storm_1/2` (`DiscMapDefaults.java`) placed by `worldgen/fog/FogStormSites.java`; storm cache loot contains `minecraft:mending` book AND custom `eclipse:replant` enchant (`data/eclipse/loot_table/fog_storm/storm_cache.json`, `enchant/ReplantEnchant.java`, `data/eclipse/enchantment/replant.json`); strong fog mobs `entity/fog/` (Fog Colossus, Fog Revenant, Storm Hound + elite goals); storm walls/interior FX `stormfx/`. |
| 59 | EMI hides gated mods' items incl dev items | **DONE** | `client/emi/EclipseEmiPlugin.java` (`@EmiEntrypoint`) — hides gated/dev content from EMI index; live re-index on unlock payloads via `client/emi/EmiReindexer.java`; recipe locks synced by `S2CRecipeLocksPayload` (`progression/RecipeGate.java`). |
| 60 | Villagers: no lectern profession, no enchanted-book trades, wandering trader disabled | **DONE** | `villagers/VillagerRestrictions.java` (`@EventBusSubscriber`) — librarian block + demotion, enchanted-book trade strip, natural wandering-trader spawn disable (config-ruled via `ProtectionConfig`). |
| 61 | Custom textures/animations | **DONE** | Final art under `assets/eclipse/textures/` (block/entity/environment/gui/item/particle), GeckoLib `geo/` + `animations/` sets, title panorama/logo, purple sun, per root `AGENTS.md` these are final generated assets, not placeholders. |
| 62 | Modular code | **DONE** | ~60 single-purpose packages with `package-info.java` contracts (admin, analytics, artifact, awards, border, buffs, cutscene, devtools, economy, ferryman, ghosts, glitch, hearts, limbo, offering, progression, ritual, sequence, skills, stormfx, timeline, veilfx, worldgen, xboxevent, …); seams/APIs (e.g. `TimedBuffApi`, `WorldStageAccess`, `RebirthApi`) instead of cross-package reach-ins. |
| 63 | Music system | **DONE** | `music/MusicManager.java` (+ `MusicCues`, `MusicConfig`, `MusicPayloads`, client hooks), sounds under `assets/eclipse/sounds/music/` with `validate_oggs.py` tooling, `/dev music` commands (`devtools/dev/DevMusicCommands.java`), Xbox era hook `xboxevent/XboxMusicHook.java`. |
| 64 | Map-expansion eclipse shader | **DONE** | Expansion ramps the eclipse post grade: `S2CEclipsePhasePayload` BUILDUP → TOTAL → ENDING around the growth sweep (`sequence/ExpansionSequence.java` lines ~330/355/599) rendered by the consolidated world grade (`veilfx/VeilPostController.java`, `assets/eclipse/pinwheel/shaders/program/world_grade.fsh`) + sky scale-up (`client/sky/OverworldPurpleEffects.java`). |
| 65 | Personal quests with small bonuses | **DONE** | Personal pool in `config/eclipse/quests.json`, `personalPerDay = 3` per player, personal section in the TAB card (`progression/goals/GoalConfig.java`, `QuestEngine.java`, `client/hud/SidebarExpanded.java` kind 2). |
| 66 | Custom XP/leveling + mini skill tree (evolved from 2 % XP / 1 % double drops) | **DONE** | Full skills stack: `skills/SkillService.java`, `SkillCurve.java`, `SkillTree.java` + `SkillTreeConfig.java`, perks incl. percentage magnitudes (`SkillPerks.java`), advancement XP bridge, rebirth hooks, `/dev`-adjacent `SkillCommands.java`; client button `client/skills/InventorySkillButton.java`. |
| 67 | Xbox 360 tutorial world event (TU12+, classic no-craft blocks, death-safe, lockout, glitch transitions, timer, rewards) | **DONE** | Era worlds tu1/tu12/tu14 (+tu19/31/69/75 manifest) (`xboxevent/XboxDimensions.java`, `XboxWorldsManifest.java`, `data/eclipse/xboxworlds/`); classic block set registered with "never: recipes, fuel, vanilla tabs" (`classicblocks/ClassicBlockList.java`); death cancel at HIGHEST priority — no item/life loss (`XboxEventService.java`); per-instance exit lockouts (`ExitReason.DEATH_LOCKED`); glitch portal transitions (`XboxTransitionBridge.java` + `PortalTransitionController`); 30:00 timer layer (`client/hud/XboxTimerLayer.java`); participation reward via `TimedBuffApi`; era post shader `xbox_era.fsh`; `/dev xboxevent` commands. |
| 68 | Configurable XP | **DONE** | `skills/SkillConfig.java` — config-file XP sources (mining/exploring/etc.), curve base/exponent/softcap, daily caps; live-reloadable; per root `AGENTS.md` config-defaults regenerate on file delete. |
| 69 | Intro animation: smoke spawn, lightning kickback, floating altar island | **DONE** | `sequence/IntroSequence.java` — smoke/storm vortex over the hidden spawn altar (`StormRegistry.spawnVortex`), `IntroLightningPhase` ramping strikes with kickback (day-1-containment clamped), BURST giant strike + shockwave, then `intro_v3_reveal` orbit of the floating altar island + sunrise with permanent purple rim. |
| 70 | Scoreboard no player count | **DONE** | `client/hud/SidebarPanel.java` class contract: "it never computes or displays an online-player count"; server aggregate payload only (`hud/SidebarSyncService.java`). |
| 71 | Logout ghosts: visible, unhittable, glitch nametag, purple skin glowing heart | **DONE** | `ghosts/LogoutGhostService.java` + `LogoutGhostEntity.java` (`setInvulnerable(true)`, no-AI, synced skin); renderer with 40 % translucency, scrambled-then-resolving glitch nametag via `GlitchText`, fullbright purple heart/veins glow layer (`client/entity/ghost/GhostPlayerRenderer.java`, `HeartGlowLayer` over `eclipsed_player_glow.png`). |
| 72 | Scoreboard rework: position/scale/on-off, TAB expansion | **DONE** | LEFT/RIGHT anchor (`EclipseClientConfig.SIDEBAR_SIDE`), scale slider 0.6–1.4 + show/hide toggle (`client/menu/SettingsPanel.java` lines ~107/114), TAB-hold expanded detail card morphing toward center (`client/hud/SidebarPanel.java`, `SidebarExpanded.java`). |
| 73 | Day-1 island containment + bedrock bounce | **DONE** | `progression/ContainmentService.java` — configurable day-gated underside containment: fall below `bounceY` → damped upward bounce, no fall damage, anticipation band FX + warning chime, containment FX payload; limbo-side watchdog bounces illegal dimension entries (`limbo/LimboGate.java`). |
| 74 | Chromatic aberration near altar | **DONE** | `client/AltarAberration.java` — smoothed proximity zone around the synced altar anchor feeding the `altar_aberration` Veil post pipeline (`assets/eclipse/pinwheel/shaders/program/altar_aberration.fsh/.json`). |

## PARTIAL details

- **#36 (dev-command bundle) — sub-item "timeline color"**: no manual color control for the day timer/timeline exists (no command in the `/dev` registry or `docs/DEV_COMMANDS.md`, no client setting, no payload field). What exists is the automatic urgency color ramp of the countdown (`client/hud/DayTimerLayer.java`: TEXT → ACCENT below 50 % remaining → ACCENT_DEEP inside the final 10 %). Every other sub-item of #36 (anti-abuse timed buffs, animated timer pause/add/sub/set, server-side render distance with client opt-out) is fully implemented and wired. If the auto-ramp was the accepted interpretation of "timeline color", reclassify #36 as DONE.

## Cross-cutting caveats (informational, not gaps)

- **#47** is DONE by supersession: the later 1:1-nether redesign (item #13, IDEA-17/W4-NETHER) intentionally replaced the 8×-smaller nether border; nether uses its own stage radii + soft border ring.
- **#48** is implemented as an admin ITEM (Display Wand + `/dev display`) rather than a literal placeable block; the repo documents it as the supported display editor.
- **#51**: removal held for all new/default saves; legacy saves whose frozen `stages.json` already lists `eclipse:stronghold_emergence` deliberately keep it (accepted in PLAN-B B15 and EVAL-SAT-S).
