# EVAL-V6-COMPLETE — V6 gap-fix verification + fresh-eyes sweep

**Evaluator:** EVAL-COMPLETE (Sol)  
**Method:** static, read-only mechanism-and-wiring review of the current working tree. Per brief, no Gradle and no git were run; this report is the only file written. A pass requires the runtime entry point, state/payload path, and consumer/resource to connect—not merely a class or constant.

## Executive verdict

- **A — 12 claimed GAP-FIX items:** **9 OK · 3 BROKEN**
- **B — 15-feature random fresh-eyes sample:** **12 OK · 3 BROKEN**
- Newly exposed defects: the storm-shatter sound has no shipped audio assets; the day-3 `create_kinetics_built` main has no producer; Photon has no shipped `.fx` payload assets.

## A. Claimed GAP-FIX items

| # | Claimed fix | Verdict | Current-tree evidence |
|---|---|---|---|
| 1 | `/dev timer color` | **OK** | Command registration, permission gate, validation, persistent write, and immediate broadcast are connected in `src/main/java/dev/projecteclipse/eclipse/devtools/dev/DevTimerCommands.java:62-91,203-226`. `RealtimeState` loads/saves and dirties `timerColorMode` at `src/main/java/dev/projecteclipse/eclipse/progression/realtime/RealtimeState.java:31,58-61,70-99,183-190`. The field is encoded in `S2CDayClockPayload` (`src/main/java/dev/projecteclipse/eclipse/network/S2CDayClockPayload.java:18-48`), the payload is registered and consumed (`src/main/java/dev/projecteclipse/eclipse/network/EclipsePayloads.java:48,248-256`), and `DayTimerLayer` applies presets/hex or the auto urgency ramp (`src/main/java/dev/projecteclipse/eclipse/client/hud/DayTimerLayer.java:296-337`). |
| 2 | `storm_heart` item + Fog Tyrant drop | **BROKEN** | Registry and drop wiring landed: `EclipseItems.STORM_HEART` is registered at `src/main/java/dev/projecteclipse/eclipse/registry/EclipseItems.java:151-163`, the item registry is attached at `src/main/java/dev/projecteclipse/eclipse/EclipseMod.java:35-44`, and Fog Tyrant death drops it at `src/main/java/dev/projecteclipse/eclipse/entity/boss/fog/FogTyrantEntity.java:1357-1375`. However, its model points to `eclipse:item/storm_heart` (`src/main/resources/assets/eclipse/models/item/storm_heart.json:1-6`) and no `assets/eclipse/textures/item/storm_heart.png` exists anywhere in the tree. The reward works mechanically but renders as a missing-texture item, so the claimed user-facing item did not land correctly. |
| 3 | Rebirth cosmetic-aura keepsake | **OK** | A completed transaction records the new rebirth before ceremony/sync (`src/main/java/dev/projecteclipse/eclipse/rebirth/RebirthService.java:131-149`). Persistent state defaults the toggle on, requires `count > 0`, and saves it (`src/main/java/dev/projecteclipse/eclipse/rebirth/RebirthState.java:31-42,65-89,97-133`). The automatically subscribed server tick emits the visible WITCH ring for eligible reborn players (`src/main/java/dev/projecteclipse/eclipse/rebirth/RebirthAuraService.java:28-64`). `/skills aura on|off` is registered and mutates/syncs that state (`src/main/java/dev/projecteclipse/eclipse/skills/SkillCommands.java:52-54,121-134`; `src/main/java/dev/projecteclipse/eclipse/rebirth/RebirthService.java:152-159`). |
| 4 | Quest `rewardXp` chips | **OK** | `rewardXp` is a real encoded/decoded payload field (`src/main/java/dev/projecteclipse/eclipse/network/S2CQuestStatePayload.java:16-67`), populated from `GoalSpec.reward().skillXp()` (`src/main/java/dev/projecteclipse/eclipse/progression/goals/QuestEngine.java:864-891`), stored by the registered client handler (`src/main/java/dev/projecteclipse/eclipse/network/EclipsePayloads.java:49,259-262`), and rendered as `+NXP` in both collapsed and TAB-expanded quest rows (`src/main/java/dev/projecteclipse/eclipse/client/hud/SidebarPanel.java:242-261,376-389`; `src/main/java/dev/projecteclipse/eclipse/client/hud/SidebarExpanded.java:274-290`). |
| 5 | Heart-theft global killer cooldown + capped-killer loss freeze | **OK** | Policy now returns loss-freezing verdicts for a capped killer and an active global killer cooldown (`src/main/java/dev/projecteclipse/eclipse/lives/HeartTheftService.java:107-121,211-227`). A successful steal records killer/victim/time, and global lookup scans the persisted ledger by killer (`src/main/java/dev/projecteclipse/eclipse/lives/HeartTheftService.java:249-287,489-563`). Config parser/defaults carry `killerCooldownMinutes=45` (`src/main/java/dev/projecteclipse/eclipse/lives/HeartTheftService.java:418-454`). The death owner calls `evaluate` before decrementing and returns without victim loss for freeze verdicts (`src/main/java/dev/projecteclipse/eclipse/lives/LifecycleEvents.java:87-103`); completed steals record cooldown at `:110-126`. |
| 6 | Day-1 goal scope/min-day fixes | **OK** | Defaults are migrated with `CONFIG_VERSION=3` specifically for this change (`src/main/java/dev/projecteclipse/eclipse/progression/goals/GoalConfig.java:46-61`). `d01_stone_age` and `d01_touch_altar` are now `EACH_PLAYER` (`src/main/java/dev/projecteclipse/eclipse/progression/goals/GoalConfig.java:545-556`), while `p_leaper` and `p_swimmer` now have `minDay=2` (`src/main/java/dev/projecteclipse/eclipse/progression/goals/GoalConfig.java:878-883`). |
| 7 | Collection cap + early SP moves | **BROKEN** | The source requirement is `dailyCreditCap 1500` for cobblestone and an early +1 SP on coal and timber (`docs/plans_v3/plans_v5/EVAL-DOPA-F.md:274-275`). Coal T2 and timber T3 correctly gained 1 point (`src/main/java/dev/projecteclipse/eclipse/collections/CollectionsConfig.java:280-289,356-365`), but cobblestone ships **600**, not 1500 (`src/main/java/dev/projecteclipse/eclipse/collections/CollectionsConfig.java:267-279`). Additionally, `CollectionsConfig.reload` only writes defaults when the file is absent and has no config-version migration (`src/main/java/dev/projecteclipse/eclipse/collections/CollectionsConfig.java:114-143`), so existing `collections.json` files retain both old cap and old tier values. |
| 8 | Collection tier crash journal | **BROKEN** | A durable pre-grant claim/journal was added and force-flushed (`src/main/java/dev/projecteclipse/eclipse/collections/CollectionsService.java:232-268`; journal serialization in `src/main/java/dev/projecteclipse/eclipse/collections/CollectionsState.java:43-73,95-124`), but it does **not** provide the claimed exactly-once cross-file transaction. `applyTierGrant` mutates skills/shards (`CollectionsService.java:276-297`), then the journal is cleared only in memory with `setDirty` and no destination receipt or ordered durability barrier (`CollectionsService.java:263-268`). Recovery likewise applies a non-idempotent payout before clearing an unflushed journal (`CollectionsService.java:300-334`). A torn later save can persist the payout but not the cleared journal (duplicate on replay), or persist the cleared journal but not the payout (lost reward). Atomic writes per file do not make several SavedData files atomic together. |
| 9 | Clear `PENDING_HEART_LOSSES` at server stop | **OK** | `LifecycleEvents` is an automatic event subscriber (`src/main/java/dev/projecteclipse/eclipse/lives/LifecycleEvents.java:31-52`); its `ServerStoppedEvent` handler clears the session-static map (`src/main/java/dev/projecteclipse/eclipse/lives/LifecycleEvents.java:211-218`). |
| 10 | `limbo.fsh` facing/epsilon/`VoyageOffset` fixes | **OK** | Epsilon growth is capped at 1.65, both camera-height and downward-ray facing gates are smoothsteps, and the mask consumes `VoyageOffset` (`src/main/resources/assets/eclipse/pinwheel/shaders/program/limbo.fsh:126-149`). `LimboAmbience` now derives voyage drift continuously from limbo-entry time rather than the hourly `Time` modulo (`src/main/java/dev/projecteclipse/eclipse/veilfx/LimboAmbience.java:326-333,364-371,465-471`). |
| 11 | Credits white-screen shadowless text | **OK** | The credits-white branch calls `renderCreditsWhite` (`src/main/java/dev/projecteclipse/eclipse/client/loading/EclipseLoadingScreen.java:148-151`); all fake-loading text there uses `drawCenteredNoShadow`, whose `drawString(..., false)` explicitly disables shadow (`src/main/java/dev/projecteclipse/eclipse/client/loading/EclipseLoadingScreen.java:162-197`). |
| 12 | German micro-fixes | **OK** | Arena copy now uses German “ausgeschaltet” throughout and the plural “Macht euch bereit…” (`src/main/resources/assets/eclipse/lang/de_de.json:1335-1352`); the rift-drone subtitle is now “Ein Riss ächzt” (`src/main/resources/assets/eclipse/lang/de_de.json:2055-2058`). The stale “Takedown(s)”, singular “Mach dich bereit…”, and “Ein Riss ächzt auf” strings are absent. |

### A defects to fix

1. Add the missing `src/main/resources/assets/eclipse/textures/item/storm_heart.png`.
2. Change cobblestone `dailyCreditCap` from 600 to 1500 and add a migration strategy if the fix must reach existing servers.
3. Make collection tier payouts idempotent at the destination (stable payout receipt), or otherwise atomically confirm payout + journal clear across all affected SavedData files; the current journal can still duplicate or lose rewards on a torn cross-file save.

## B. Fresh-eyes random completeness sweep

Selection used fixed seed **20260725** over an enumerated pool of 50 non-headliner rows marked DONE in `AUDIT-recent.md`. The raw draw contained the semantic duplicate D8/POL-S3; it was counted once. A7 was excluded as a headline UI item, and the next draws (B9, D12) were taken. The order below is the resulting 15-feature sample.

| # | Random DONE feature | Verdict | End-to-end trace |
|---|---|---|---|
| 1 | v4 arena armor phase | **OK** | Arena portal entry clears inventory, routes arena entrants to `ArenaGame.giveKit`, and grants spawn protection (`src/main/java/dev/projecteclipse/eclipse/minigames/MinigameService.java:510-523`). `giveKit` equips all four armor slots and adds weapons/ammo (`src/main/java/dev/projecteclipse/eclipse/minigames/ArenaGame.java:156-167`); round reset clears and reapplies it (`:379-390`). |
| 2 | B17 OP-only `/give` names | **OK** | The client mixin is enabled in `src/main/resources/eclipse.client.mixins.json:6-11`; it returns no names below gamemaster permission but leaves vanilla suggestions intact for OPs (`src/main/java/dev/projecteclipse/eclipse/client/mixin/ClientSuggestionProviderMixin.java:24-43`). The server-side fallback `/dev player names` is permission-2 gated and sends the real roster only to its source (`src/main/java/dev/projecteclipse/eclipse/devtools/dev/DevPlayerCommands.java:58-74,102-126`). |
| 3 | POL-S3 nested-id allowlist migration | **OK** | Old/missing-version files parse as v1, are union-merged to v2, and are saved (`src/main/java/dev/projecteclipse/eclipse/admin/AntiCheatCheck.java:179-241,569-576`). Nested Fabric/MixinSquared/Photon ids exist in shipped allowed + optional defaults (`:683-712`), and live mod reports are evaluated/enforced at `:461-493`. |
| 4 | B1 mountain snowline persistence | **OK** | The biome-source codec is registered (`src/main/java/dev/projecteclipse/eclipse/registry/EclipseWorldgen.java:21-39`) and instantiated by the live disc chunk generator (`src/main/java/dev/projecteclipse/eclipse/worldgen/DiscChunkGenerator.java:67-81`). Noise-biome lookup maps the whole mountain footprint at/above Y 152 to snowy slopes (`src/main/java/dev/projecteclipse/eclipse/worldgen/DiscBiomeSource.java:184-216`), so precipitation/freezing sees the corrected biome. |
| 5 | A2 loading-screen fade-out | **OK** | Opening vanilla receive/preparation screens are replaced by the subscribed swap (`src/main/java/dev/projecteclipse/eclipse/client/loading/LoadingScreenSwap.java:32-50`). Normal removal arms the fade (`src/main/java/dev/projecteclipse/eclipse/client/loading/EclipseLoadingScreen.java:100-119`), and subscribed GUI/screen render hooks replay the final panel with cubic alpha until completion (`:328-385`). |
| 6 | POL-F5 dedicated storm-shatter sound weight | **BROKEN** | The tyrant death beat calls the registered `EVENT_STORM_SHATTER` (`src/main/java/dev/projecteclipse/eclipse/entity/boss/fog/FogTyrantEntity.java:1406-1426`; registration at `src/main/java/dev/projecteclipse/eclipse/registry/EclipseSounds.java:213-217`). `sounds.json` gives it its own layered entry (`src/main/resources/assets/eclipse/sounds.json:380-399`), but those layers reference `eclipse:ui/heart_shatter`, `eclipse:event/submerge`, and `eclipse:ambient/limbo_loop`, while a complete tree scan finds **zero `.ogg` files**. The event fires but resolves to missing audio. |
| 7 | C4 pre-event limbo safety + ghost revival | **OK** | The subscribed safety owner cancels incoming damage/death before the normal life-decrement handler and runs tick-driven water rescue (`src/main/java/dev/projecteclipse/eclipse/limbo/PreEventSafety.java:69-131,136-217`). `LivesApi.set` detects a banned 0→positive transition, invokes the standard unban, restores the intended count, and fires revive effects (`src/main/java/dev/projecteclipse/eclipse/core/state/LivesApi.java:40-58`). |
| 8 | v4 Sonic0810 rights | **OK** | Defaults contain `name:Sonic0810` (`src/main/java/dev/projecteclipse/eclipse/admin/AntiCheatCheck.java:713-721`); name pins resolve against the online player/profile cache (`:315-359`). The match bypasses mismatch and timeout enforcement (`:461-489,511-533`) and grants only the shared `/dev` perm-2 surface, never OP (`src/main/java/dev/projecteclipse/eclipse/devtools/dev/DevRoot.java:50-83`). |
| 9 | B4 villages no longer float | **OK** | The village pending placer is registered and routed through plateau-mode placement (`src/main/java/dev/projecteclipse/eclipse/worldgen/structure/StructureStamper.java:175-187,215-229`). Vanilla pieces are vertically translated to deterministic terrain height before `SitePrep.preparePlateau`, then place only after prep completes (`src/main/java/dev/projecteclipse/eclipse/worldgen/structure/VanillaLandmarks.java:118-179`). |
| 10 | v4 day-3 tasks | **BROKEN** | Day 3 authors forge, kinetics, and exploration mains (`src/main/java/dev/projecteclipse/eclipse/progression/goals/GoalConfig.java:585-606`). Craft/exploration signals genuinely reach quest increments (`src/main/java/dev/projecteclipse/eclipse/progression/goals/QuestDetectors.java:48-70,115-143`), but `d03_kinetics` depends on external beat `create_kinetics_built` (`GoalConfig.java:590-592`). `QuestEngine.evaluateBuiltinBeat` returns false for unknown/external beats (`src/main/java/dev/projecteclipse/eclipse/progression/goals/QuestEngine.java:650-696`), and a complete Java-tree search finds no `completeTeamBeat(..., "create_kinetics_built")` producer. One of the three day-3 mains is impossible in normal play. |
| 11 | C5 intro storm push/day switch/no free night credit | **OK** | The live lightning controller calls the push zone every tick and applies synchronized outward motion/slow fall (`src/main/java/dev/projecteclipse/eclipse/sequence/IntroLightningPhase.java:138-164,268-309`). Burst advances time forward to morning and records the jump tick (`src/main/java/dev/projecteclipse/eclipse/sequence/IntroSequence.java:578-615`). The quest poll consumes that marker and voids a dawn inside the 200-tick grace instead of crediting survivors (`src/main/java/dev/projecteclipse/eclipse/progression/goals/QuestDetectors.java:320-367`). |
| 12 | POL-S2 budgeted credits beach stamp | **OK** | Finale completion calls `CreditsSequence.begin` (`src/main/java/dev/projecteclipse/eclipse/ritual/FinaleRitual.java:218-230`); begin queues beach work immediately (`src/main/java/dev/projecteclipse/eclipse/ritual/CreditsSequence.java:344-390`). The resumable cursor is advanced through `BudgetedBlockWriter` (`:1151-1235`), whose subscribed post-tick dispatcher slices queued jobs under the shared deadline (`src/main/java/dev/projecteclipse/eclipse/worldgen/stage/BudgetedBlockWriter.java:33-38,70-108`). |
| 13 | POL-F6 altar surge echo clock | **OK** | Altar milestone completion broadcasts `FX_ALTAR_LEVELUP` (`src/main/java/dev/projecteclipse/eclipse/ritual/AltarBlockEntity.java:199-208`); the payload handler starts `AltarCeremonyFx` (`src/main/java/dev/projecteclipse/eclipse/network/fx/FxPayloads.java:178-186`). L5 sets `surgeStart`, and `skySurgeEchoTravel` advances monotonically 0→1 (`src/main/java/dev/projecteclipse/eclipse/client/drama/AltarCeremonyFx.java:195-204,226-259`). The live sky renderer consumes that travel value and expands/fades one ring (`src/main/java/dev/projecteclipse/eclipse/client/sky/AltarVeilSky.java:158-203,432-457`). |
| 14 | B9 earlier border kick + 5 s slow fall | **OK** | The subscribed player tick warns inside R−4, impulses in R..R+1.5, and teleports beyond that (`src/main/java/dev/projecteclipse/eclipse/border/SoftBorder.java:447-494`). Teleport fallback searches inward for two solid columns and grants 100 ticks of Slow Falling on either landing path (`:516-586`). |
| 15 | D12 Photon adoption | **BROKEN** | The client bridge is genuinely called from the Quasar payload path and rift path (`src/main/java/dev/projecteclipse/eclipse/network/EclipsePayloads.java:148-157`; `src/main/java/dev/projecteclipse/eclipse/veilfx/rift/RiftFx.java:200-219`). It correctly gates on installed Photon/settings and resolves Photon only through reflection (`src/main/java/dev/projecteclipse/eclipse/veilfx/PhotonBridge.java:282-300,700-730`). However, the bridge expects drop-in `assets/eclipse/fx/<id>.fx` resources (`PhotonBridge.java:78-81`) and treats a null asset as a failed spawn (`:473-497`), while a complete tree scan finds **zero `.fx` files**. `docs/BUNDLING.md:143-148` explicitly says only hook points ship and authors must supply a resource pack; that makes the optional authoring seam intentional, but it does not make the advertised user-facing Photon enhancement work end-to-end in the shipped product. (It also conflicts with the later claim in `docs/plans_v3/plans_v5/EVAL-V6-RACES.md:61-65` that matching assets exist.) |

### B newly found broken wiring

1. **Storm-shatter audio:** trigger and sound definition exist, but the repository ships no OGG assets, so the cue is silent.
2. **Day-3 kinetics main:** `create_kinetics_built` is declared but never fired, so `d03_kinetics` cannot complete naturally.
3. **Photon effects:** reflection bridge and call sites exist, but no `.fx` payloads ship; without a separately authored resource pack every Photon enhancement resolves missing and no-ops.
