/**
 * WAVE C — styles 20..35 (built by the wave_c style agent).
 * See wave_b.ts for the config template and rules; reference quality in
 * wave_a.ts. Style 35 (grand_finale_megamix) is the long one: 35–40s,
 * revisits the strongest scenes of all styles, gets a 4K final render
 * (scripts/render-v2.mjs --only 35 --scale 2).
 */
import type {TrailerStyleConfig} from '../config/types';

export const WAVE_C: TrailerStyleConfig[] = [];
