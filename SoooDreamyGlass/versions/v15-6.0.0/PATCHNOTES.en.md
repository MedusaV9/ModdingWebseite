# SoooDreamy 6.0.0 — Together, Anywhere

- Guided passphrase-encrypted migration of logical couple data from server A to a fresh couple on server B.
- SHA-256 content check, schema gate, 16 MB JSON bound, and atomic rejection of tampered or non-empty destinations.
- Tokens never import; the second partner re-pairs with the new code.
- The end-to-end test proves byte-equal normalized logical content before and after import and re-pairing.

Honest limit: a server admin must copy large media binaries separately from the data directory. IPA unsigned; device verified: no.

SoooDreamy — made by Sonic0810
