# SoooDreamy 4.6.0 — Backup, Export & Restore

- Four independent export domains and three independent restore domains.
- Schema-2 manifest with deterministic legacy migration and future-schema blocking.
- AES-GCM/PBKDF2 remains default; tokens and passphrases stay out.
- Tested round-trip, selection contract, and restore rollback.

Honest limit: couple snapshot is read-only; no CloudKit device test. IPA unsigned.

SoooDreamy — made by Sonic0810
